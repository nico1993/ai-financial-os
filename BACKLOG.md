# Personal Financial OS — Backlog

Stories grouped by epic, derived from `ARCHITECTURE.md`. Each story is a checklist item; check them off as they land. IDs (`SETUP-1`, `ING-3`, etc.) are stable references for commit messages, PR titles, or code comments — cheap to reference without a full task tracker.

**TDD convention:** for any story implementing a pure logic function (categorization resolvers, transfer-matching, rollup bucket-tracking, sync pagination), write the test first. Stories that primarily wire a pure function into a queue/route are thin by design and need less test investment than the logic they call.

## Suggested Sequencing

Epics have real dependencies — this is the order that avoids building on top of nothing:

1. **SETUP** — nothing else can start without the monorepo, tooling, and a Docker Compose skeleton in place.
2. **DATA** — schemas and the repository layer come before anything that needs to persist or query data.
3. **AUTH** — `apps/api` needs a real "current user" per request before any route (starting with ING-3's Link routes) can be written correctly (ADR-0018).
4. **ING** — ingestion needs DATA's schemas to write into, and AUTH's `requireAuth` to know whose data it's writing.
5. **CAT** — categorization needs ING's transactions to categorize. _Resequenced 2026-08-31: CAT-7 specifically (the review queue UI) also needs WEB-7/WEB-8 (connect-account flow + a transactions list page) to exist first -- see those tickets. The other nine CAT items are backend-only and unaffected._
6. **XFER** — transfer matching needs CAT's category signals (`TRANSFER_*`) to find candidates. _Correction (ADR-0029): the actual signal is `Transaction.providerCategory`, Plaid's raw field set at sync time -- not any CAT-9 taxonomy value. XFER-2 still chains off categorize-llm's completion to keep the documented pipeline order, not because matching needs Tier 1-4's output._
7. **WEB** — `apps/web` is still SETUP-1's empty stub (package.json/tsconfig/eslint config, no framework, no pages). Bootstrap the application shell -- framework, routing, API client, auth guard, design system -- before any dashboard or review-queue page can be built on top of it. Only depends on AUTH's session contract (ADR-0018) being real, not on ANLY's data existing yet, so it can proceed in parallel with (or right after) the backend work above.
8. **ANLY** — dashboards need ING + CAT + XFER's data (backend) and WEB's application shell (frontend) to be meaningful (cash flow needs `excludeFromCashFlow` from XFER, categories from CAT, transactions from ING, a page to render into from WEB).
9. **SEC** — baseline network/secrets posture belongs in SETUP; the items listed under SEC here are the ones worth deferring slightly (encryption at rest, backups) since they don't block functional development, but shouldn't slip past Phase 1 sign-off.
10. **Phase 2 stories** (`ACCT`, plus new stories appended to `CAT`/`XFER`/`WEB` below) — added 2026-09-01, all net-new feature requests layered on an otherwise-complete Phase 1 (everything above is done except `SEC`). No cross-epic dependency chain like 1-9 above; each new story only depends on what its own epic already built. One sequencing call worth making deliberately: settle `WEB-11` (the redesign pass) before building the other UI-heavy ones (`ACCT-1`, `CAT-11`/`CAT-12`, `XFER-7`, `WEB-9`/`WEB-10`), so new UI gets built once in whatever direction that lands on, not twice.

---

## Epic: SETUP — Repo & Tooling Foundation

- [x] **SETUP-1** — Scaffold pnpm + Turborepo monorepo: `apps/{web,api,worker}`, `packages/{db,providers,shared,config}` (section 7.1).
- [x] **SETUP-2** — Shared `tsconfig.base.json` in `packages/config` — `strict: true`, `noUncheckedIndexedAccess` — extended by every workspace (section 7.3).
- [x] **SETUP-3** — Shared ESLint + Prettier config in `packages/config`: `typescript-eslint` base, `eslint-plugin-react`/`eslint-plugin-react-hooks` for `apps/web`, plain Node/TS ruleset for `apps/api` and `apps/worker` (section 7.4).
- [x] **SETUP-4** — Husky + lint-staged: lint and format on every commit.
- [x] **SETUP-5** — Vitest configured per workspace with a shared base config in `packages/config` (section 7.5).
- [x] **SETUP-6** — GitHub Actions CI workflow: lint + typecheck + test on every push.
- [x] **SETUP-7** — Docker Compose skeleton: containers for `web`, `api`, `worker`, MongoDB, Redis, reverse proxy — no app logic yet, just scaffolding and healthchecks (section 5). _Addendum (2026-08-31): `web`'s healthcheck is a stub (`exit 0`, always passes) that never actually probes the Vite dev server -- confirmed the hard way during this session's first real `docker compose up --build` run, where `web-1` showed green/healthy in Docker Desktop for 12+ minutes while it was in fact unreachable (see ANLY-9's addendum for the actual bug). Not fixed yet; tracked here rather than left implicit -- worth a real check (e.g. `wget --spider` against the dev server's own port) once there's a static build to check instead (per the Dockerfile's own TODO)._
- [x] **SETUP-8** — `.env.example` + secrets-loading convention documented (dotenv for dev; Docker secrets / mounted file for prod).
- [x] **SETUP-9** — Reverse proxy (Caddy/Traefik) config: TLS termination, routing to `web`/`api`; confirm Mongo/Redis are bound to the Docker internal network only.
- [x] **SETUP-10** — `README.md`: quick orientation — setup steps, repo layout, how to run locally.

## Epic: DATA — Data Model & Persistence

- [x] **DATA-1** — `Connection` schema (`packages/db`) (section 3.2).
- [x] **DATA-2** — `Account` schema.
- [x] **DATA-3** — `Transaction` schema, including `category`, `transferGroupId`, `excludeFromCashFlow`, `isRemoved`.
- [x] **DATA-4** — `RawPayloads` schema (insert-only, `source`/`type` discriminator) (section 3.1).
- [x] **DATA-5** — `MerchantRules` schema (Tier 1/2 rules, `priority` field).
- [x] **DATA-6** — `DailyBalanceSnapshot` / `MonthlyRollup` schemas.
- [x] **DATA-7** — `Subscriptions` schema.
- [x] **DATA-8** — Apply the six indexes from section 3.2 (`userId+date`, `userId+category.value+date`, `providerTransactionId` unique, `accountId+date`, `userId+category.status`, `transferGroupId` sparse).
- [x] **DATA-9** — Repository layer: `ConnectionRepository`, `AccountRepository`, `TransactionRepository`, `RollupRepository` with domain-shaped methods (`upsertFromSync()`, `findByUserAndDateRange()`, `getMonthlyRollup()`) — no raw driver calls outside repositories (section 3.3, ADR-0005). _`RawPayloadRepository` was missing from this list even though §3.1 requires the collection to be written; added during ING-4, see ADR-0021._
- [x] **DATA-10** — Integration tests for repositories and aggregation pipelines using `mongodb-memory-server`, written alongside each repository method.

## Epic: AUTH — Session-Based Authentication (ADR-0018)

- [x] **AUTH-1** — `User` schema (`packages/db`): `email` (unique), `passwordHash` (`select: false`); `UserRepository` with `create`/`findByEmail`/`findByEmailWithPassword`/`findById`/`count`.
- [x] **AUTH-2** — Redis-backed session plugin in `apps/api` (`@fastify/cookie` + `@fastify/session`, hand-written `RedisSessionStore` on top of `ioredis` rather than `connect-redis` — avoids betting on that package's exact version/typings for a ~30-line get/set/destroy store) — `httpOnly` cookie, `secure` in production.
- [x] **AUTH-3** — Auth routes: `POST /api/auth/register` (allowed only when zero users exist yet, or the caller is already authenticated), `POST /api/auth/login`, `POST /api/auth/logout`, `GET /api/auth/me`.
- [x] **AUTH-4** — `requireAuth` preHandler hook applied to every route except `/api/auth/*`, the Plaid webhook receiver (ING-7, verified via JWT instead), and `/health`.
- [ ] **AUTH-5** — `apps/web` login page + redirect-if-unauthenticated. Deferred until `apps/web` work starts — not needed to unblock the backend. _Implemented as part of WEB-4 (application shell), once the WEB epic bootstraps `apps/web`; this id stays here since it's the stable reference already used elsewhere._
- [x] **AUTH-6** — `apps/web` bootstrap registration page. _Gap found 2026-08-31, while trying to actually sign in against a fresh deploy: the backend's `POST /api/auth/register` (AUTH-3) only ever accepts a call while zero users exist yet, or from an already-authenticated session adding another -- deliberately not a public signup endpoint (ADR-0018). No ticket ever named a *frontend page* for it, though -- AUTH-5/WEB-4 built only the login form, and a grep of `apps/web/src` turns up zero references to register anywhere (no form, no route, no link). Net effect: on a genuinely fresh database (this session's first real `docker compose up --build`, empty Mongo volume) there is currently no way to create the one bootstrap account through the browser at all. Worked around for now with a direct `curl` call against `/api/auth/register`; a real page (or at minimum a documented one-time CLI/seed script) is worth building before this app is handed to anyone less comfortable doing that by hand._ **Delivered (2026-08-31)**: found again independently by a pre-validation self-audit before this fix landed -- same gap, confirmed still open. `pages/RegisterPage.tsx` (email + password + confirm-password, client-side mismatch check, posts to `/api/auth/register` via a new `useRegisterMutation()` in `auth/session.ts`), routed at `/register` (public, outside `ProtectedRoute`, alongside `/login`). Always reachable rather than conditionally shown -- there is no cheap public way to know in advance whether bootstrap is still open, and the backend's existing `403 "registration is closed"` is already a clear answer post-bootstrap. `LoginPage.tsx` gained a permanent "Create an account" link. Full reasoning in ADR-0042. **Verified**: `apps/web`'s `tsc --noEmit`/`eslint .` clean. **Unverified**: the actual browser flow (create the bootstrap account, land on `/accounts` signed in, then confirm a second `/register` visit correctly 403s) -- not clicked through from this session._

## Epic: ING — Data Ingestion & Provider Abstraction

- [x] **ING-1** — `FinancialProvider` interface in `packages/providers` (section 2.1, ADR-0004).
- [x] **ING-2** — `PlaidProvider` adapter implementing `FinancialProvider`.
- [x] **ING-3** — Plaid Link flow: public token exchange, `Connection` creation, initial `Account` fetch, 30-day backfill via `days_requested` (ADR-0002). _Addendum (2026-08-31): "30-day backfill via `days_requested`" describes what `createLinkToken()` asks Plaid's consent screen to authorize -- it does NOT mean this story pulls transactions in. `createConnection()` (`packages/providers/src/plaid/PlaidProvider.ts`) only ever calls `accountsGet` (balances), never `transactionsSync` -- confirmed by a pre-validation self-audit reading the actual implementation, not assumed from this ticket's own wording. Transaction data only ever arrives via the `provider-sync` queue (ING-4); see ING-13 for the gap that left nothing triggering it right after Link finishes._
- [x] **ING-4** — `provider-sync` BullMQ queue + job handler: cursor-based pagination loop, cursor persisted after each page (section 2.2).
- [x] **ING-5** — Idempotency lock per `connectionId` (Redis lock or BullMQ `jobId` dedup). _Chose `jobId` dedup via a single shared `enqueueProviderSync()` helper; see ADR-0022 for why a mutex was unnecessary, and for the `removeOnComplete` trap and the accepted mid-run-trigger limitation._
- [x] **ING-6** — Rate limiting + retry/backoff config on the `provider-sync` queue (429 handling). _Needed a provider-neutral error taxonomy first (ADR-0023) so a Plaid 429 could reach the worker without leaking `error_code`/axios details past the adapter._
- [x] **ING-7** — Plaid webhook receiver (`apps/api`): `SYNC_UPDATES_AVAILABLE` triggers a `provider-sync` job; verify webhook JWT signatures. _Needed a scoped raw-body parser (re-serializing Fastify's parsed JSON changes the bytes, so the signature hash never matches) and `FinancialProvider.parseWebhook()` (ADR-0024) to keep Plaid webhook codes out of the route._
- [x] **ING-8** — Scheduled fallback repeatable job (poll every 4–6h) as a webhook-miss safety net. _Own queue rather than a repeatable job on `provider-sync` (ADR-0025); work list read from the DB at run time, so newly linked connections need no re-registration._
- [x] **ING-9** — Pending→posted reconciliation: soft-delete/link the pending record when its posted counterpart arrives (section 6). _Also carries a real category across to the posted row, otherwise a manual Tier 4 correction is lost when the transaction settles, and records both date buckets since the pending row may sit in an earlier one._
- [x] **ING-10** — Item error-state handling: detect `ITEM_LOGIN_REQUIRED`, mark `Connection.status`, stop retrying until the user re-auths (section 6). _Handled on both paths (API error and ITEM webhook), splitting re-auth from revoked since the recoveries differ; raised as `UnrecoverableError` so BullMQ stops retrying immediately._
- [x] **ING-11** — Cursor-drift handling: detect `PLAID_ERROR` on a stale cursor, explicit reset-and-full-resync path (section 6). _Invalid-cursor (reset + resync from empty) kept distinct from mutation-during-pagination (restart the drain, keep the cursor); conflating them would force needless full resyncs._
- [x] **ING-12** — Unit tests for the sync job's pure logic (pagination/cursor handling, pending/posted merge logic), written first. _Pagination/cursor with ING-4; pending/posted and cursor-drift with ING-9/ING-11; provider error and webhook mapping alongside ING-6/ING-7. All written before their implementations._
- [x] **ING-13** — Trigger an initial `provider-sync` job right after Plaid Link exchange succeeds, plus a manual re-sync route/button. _Gap found 2026-08-31 by a pre-validation self-audit, not tracked anywhere before this: `routes/plaid.ts`'s exchange handler wrote the `Connection`/`Account` records and stopped -- `requestProviderSync()`'s only caller anywhere in the codebase was the webhook handler (`routes/webhooks.ts`). On a deployment with `PLAID_WEBHOOK_URL` unset (this repo's `.env` -- "Plaid can't reach a laptop") the only remaining trigger was ING-8's scheduled poll, `PROVIDER_SYNC_POLL_INTERVAL_MS` later (5 minutes in this `.env`, 4 hours in `.env.example`) -- a freshly linked account showed a balance and zero transactions with nothing on screen explaining why. **Delivered**: the exchange handler now calls `requestProviderSync(connection._id.toString())` immediately (try/caught -- logs rather than fails the response, since the Connection/Accounts are already persisted by that point). New `POST /api/accounts/:connectionId/sync` (ownership-checked) lets a user force a re-sync any time afterward, not just at link time. `GET /api/accounts` now returns each account's `connectionLastSyncedAt`; `AccountsPage.tsx` shows "Synced Xm ago" / "Never synced yet" plus a "Sync now" link per row, and `useAccountsQuery()` polls every 30s so a completed sync's timestamp actually appears without a manual reload. `ConnectionRepository.findById()` gained the same defensive `CastError`-to-`null` handling ADR-0041 added to `TransactionRepository.updateCategoryForUser()`, since the new sync route is the first caller to feed it a raw client-supplied id. Full reasoning in ADR-0042. **Verified**: `packages/db`/`apps/api`/`apps/worker`/`apps/web` all clean on `tsc --noEmit`/`eslint .`; `accounts/list.test.ts` extended with 2 new hand-traced cases for `connectionLastSyncedAt` (9 total). **Unverified**: whether a real Plaid Sandbox link now actually shows transactions within moments instead of after a silent wait -- not exercised end-to-end from this session._

## Epic: ACCT — Account Management

User-driven management of linked `Account` records — renaming and removing an account is something the user does from the Accounts page, distinct from ING's provider-sync concerns. _Added 2026-09-01 at the user's request; no stories existed for this before._

- [ ] **ACCT-1** — Rename a linked account (user-facing nickname). `Account` (`packages/db/src/models/Account.ts`) has no user-editable name field today — `institutionName`/`officialName` both come from Plaid and are overwritten on every sync (`AccountRepository.upsertFromSync()`'s `$set: input`, where `input` is typed as a `Pick` of provider-driven fields only). Needs a new `nickname?: string` field kept **out** of `UpsertAccountInput` so a sync can never overwrite a name the user chose; an ownership-scoped repository method and `PATCH /api/accounts/:id` route (same `{_id, userId}` pattern `CategoryRepository.updateColor()`/`TransactionRepository.updateCategoryForUser()` already use); an inline rename control on `AccountsPage.tsx`. Display becomes `nickname ?? officialName ?? institutionName`.
- [ ] **ACCT-2** — Delete (unlink) a bank account. **Open question, not decided here**: a Plaid Item (`Connection` in this schema) can own several `Account`s (checking + savings at the same bank), so "delete this account" could mean (a) hide one `Account` while its siblings and the `Connection` stay linked — needs a soft `archived`/`hidden` flag so historical `DailyBalanceSnapshot`/`MonthlyRollup` rows that reference it don't silently break net-worth/cash-flow history — or (b) fully unlink the whole `Connection` (revoke via Plaid's `/item/remove`, the voluntary counterpart to the involuntary re-auth/revoked handling ING-10 already built). Needs a product decision before either gets built; whichever is chosen, follow this codebase's existing soft-delete convention (`Transaction.isRemoved`, `Category.archived`) rather than a hard delete.

## Epic: CAT — Categorization Pipeline

- [x] **CAT-1** — Tier 1 exact-match resolver (pure function): lookup against `MerchantRules` by normalized merchant string (section 2.3).
- [x] **CAT-2** — Tier 2 regex/fuzzy resolver (pure function): priority-ordered rule evaluation + fuzzy matching against previously-corrected merchants.
- [x] **CAT-3** — Wire Tier 1 + Tier 2 inline into the sync job (no queue overhead).
- [x] **CAT-4** — `categorize-llm` BullMQ queue + job handler: batched LLM calls (20–50 tx/batch) via a `CategorizationProvider` abstraction (ADR-0026, Ollama/`gpt-oss:20b` for Phase 1), structured JSON output schema, own concurrency limit.
- [x] **CAT-5** — LLM confidence threshold + `uncertain` flag handling → `needs_review` status (`categorize/tier3.ts`'s `resolveTier3Outcome()`).
- [x] **CAT-6** — Write-back loop: cache confirmed LLM decisions into the Tier 1 `MerchantRules` table (`MerchantRuleRepository.upsertExact()`, ADR-0027). The _manual_ half of "LLM/manual" stays open -- it has no caller until CAT-7's review queue UI exists.
- [x] **CAT-7** — Tier 4 review queue UI (`apps/web`): list `needs_review` transactions, manual category correction, triggers write-back. _Rescoped 2026-08-31, once WEB-8 (transactions list page) was identified as a prerequisite: this should reuse WEB-8's transaction-row component and list page, filtered to `category.status === "needs_review"`, rather than a fully separate page built from scratch. Backend read side already exists and is unused -- `TransactionRepository.findNeedsReview()` was written for exactly this and has had no caller. Write side also already exists and is unused -- `TransactionRepository.updateCategory()`, which is what CAT-6's write-back loop needs a caller for. What's still missing either way: the routes (`GET /api/transactions?status=needs_review` or similar, `PATCH /api/transactions/:id/category`) and the frontend page itself. Depends on WEB-8 landing first._ **Delivered (2026-08-31)**: not built on `findNeedsReview()`/`updateCategory()` directly after all -- the route side needed ownership scoping neither of those provides (see below), so WEB-8's `findPageForUser({status: "needs_review"})` serves the list (already built for exactly this) and a new `TransactionRepository.updateCategoryForUser(userId, transactionId, category)` serves the correction, scoped to `{_id, userId}` together so one user can never correct another's transaction via a guessed id -- the same ownership check `CategoryRepository.updateColor()` established for CAT-10. It also catches mongoose's `CastError` on a malformed id and returns `null` rather than 500ing -- this route is the first place in `apps/api` that feeds a raw client string into a Mongo `_id` filter. New `PATCH /api/transactions/:id/category` route (`routes/transactions.ts`, `requireAuth`-gated): a manual correction always writes `{tier: 4, value: <chosen>, status: "confirmed"}` -- there's no needs_review outcome for a human's own decision -- then writes the CAT-6 write-back straight into `MerchantRuleRepository.upsertExact()` (`source: "manual"`) rather than importing `apps/worker`'s `writeBack.ts` (`apps/api` has never depended on `apps/worker`; that file's own doc comment sanctioned this exact fork). New `GET /api/categories` route (`routes/categories.ts`) backs the correction dropdown -- read-only, no create/rename/recolor route yet (out of this ticket's actual scope; see ADR-0041). Frontend: `api/categories.ts` (`useCategoriesQuery`), `api/transactions.ts` gained `useCorrectCategoryMutation()` (invalidates the `["transactions"]` prefix and `["analytics", "spending-categories"]` directly on success -- no SSE event exists for "a category changed," and none is needed since this is the same browser session that made the edit). `components/ReviewCategoryControl.tsx`: a native `<select>` of this user's active categories, falling back to a free-text input + Save button when none exist yet (Tier 3 has not run long enough to seed them) or the categories query itself errors -- both collapse to the same fallback rather than blocking correction on category-list plumbing. `pages/ReviewPage.tsx` replaces the `/review` `PageStub` -- almost entirely `TransactionsPage.tsx` with `status: "needs_review"` and `renderRowAction` filled in, per this ticket's own "reuse WEB-8's transaction-row component and list page" text. A corrected transaction simply stops matching the `needs_review` filter on refetch and disappears from the queue -- no special-case removal logic needed. Full reasoning in ADR-0041. **Verified**: `packages/db`, `apps/api`, `apps/worker` (touched only by the shared `TransactionRepository` change), and `apps/web` all clean on `tsc --noEmit`/`eslint .`. **Unverified**: real end-to-end behavior -- does the CastError guard actually catch a malformed id, does the free-text fallback path actually work against a fresh sandbox user with zero seeded categories, does a correction really disappear from the queue and really show up recategorized on the Spending page -- `vitest` still cannot run from this sandboxed session, so nothing here has been exercised beyond `tsc`/`eslint`, and the page has not been clicked through in a real browser._
- [x] **CAT-8** — Unit tests for the Tier 1/2 resolvers and the LLM response-parsing/confidence logic, written first.
- [x] **CAT-9** — Richer/per-user category taxonomy. `DEFAULT_CATEGORIES` (`apps/worker/src/categorize/categories.ts`) is currently one fixed, app-owned list of 18 categories; running CAT-4/5 against real Plaid sandbox data (a CD deposit, a loan payment, an ambiguous employer-name credit) showed several transaction shapes with no good fit, correctly landing in `needs_review` rather than being miscategorized, but pointing at two real gaps: (1) the default list itself could use more categories/subcategories (loan payments, transfers, investments are the ones the sandbox run actually hit), and (2) categories are the same for every user with no way to add/rename/hide one. `CategorizationProvider.categorizeBatch(candidates, categories)` already takes `categories` as a plain `readonly string[]` with no opinion on its contents, so the interface doesn't need to change -- this is about where that list comes from (a DB collection per user instead of a hardcoded array) and the UI to manage it, likely alongside CAT-7's review queue. **Delivered:** the per-user DB collection half (`Category` model, `CategoryRepository`, lazy per-user seeding, ADR-0028) -- `categorize-llm` now reads a user's own editable category list instead of the shared constant. **Not delivered:** the taxonomy itself is still the same 18 names carried over unchanged (no new loan-payment/transfer/investment-specific categories added); a user can only get a richer list today by adding a custom category themselves via `CategoryRepository.create()`, since no route/UI calls it yet (CAT-7).
- [x] **CAT-10** — Category colors. Save a default color into the default categories model (CAT-9), changeable by the user; custom categories a user adds get the same treatment (a color field on the same per-user category record, set on creation and editable afterward).

- [ ] **CAT-11** — Category icons. `Category` (`packages/db/src/models/Category.ts`) has `color` but no `icon`, and none of the seeded defaults (the `{name, color}` seed list `CategoryRepository.seedDefaults()` takes as `CategorySeed[]`, defined in `apps/worker/src/categorize/categories.ts`) has one either. Needs an `icon` string field on the schema (an icon name/key, same shape as `color`'s hex string), an `icon` added to `CategorySeed` and the default seed list, and a way to change it — either a new `updateIcon()` alongside `updateColor()`, or generalizing both into one `update(userId, categoryId, {color?, icon?})`, probably the better call. **Decided (2026-09-01, user confirmed)**: add `lucide-react` as a new `apps/web` dependency rather than hand-drawing more inline SVGs — it's the conventional shadcn/ui pairing, tiny and tree-shakeable, and `pnpm install` has worked from the user's own machine since WEB-7. Pick one curated, fixed list of icon names from it — not free-form icon upload — sized to cover the 18 default categories with room for custom ones to pick from the same set; store the chosen icon's string key (e.g. `"shopping-cart"`) on `Category.icon`, resolved to the actual `lucide-react` component at render time.
- [ ] **CAT-12** — Transaction edit popup: click any row (not just `needs_review` ones) to open a popup with three fields — rename description (CAT-13's override field), recategorize, and pick the category's icon (CAT-11). Rescoped 2026-09-01: the original plan here was just reusing `ReviewCategoryControl.tsx`'s inline dropdown on the general ledger, before the user asked for a fuller popup instead. The recategorize half of the original plan still holds and is most of the reusable backend: `PATCH /api/transactions/:id/category` (`routes/transactions.ts`) already works on any transaction regardless of `category.status`, so the popup's category field is mostly new UI around an existing route. The description and icon fields are new end-to-end (CAT-13, CAT-11) — this story depends on both landing first. **Decided (2026-09-01, user confirmed)**: this popup is the general ledger's edit affordance only — `ReviewCategoryControl.tsx`'s inline dropdown on the Review queue stays exactly as-is, since triaging many `needs_review` items benefits from the faster one-click flow. **Still open**: whether the popup's category field can create a new category inline (CAT-14) rather than only picking an existing one, since that's the natural place a user would reach for it.
- [ ] **CAT-13** — Editable merchant display name. **Decided (2026-09-01, user confirmed)**: the safe, display-only version. Add a `merchantNameOverride?: string` field on `Transaction` that wins the existing `merchantName ?? merchantNameNormalized` fallback (`buildTransactionList()`, behind `TransactionsTable.tsx`'s Merchant column) once a user sets it. `merchantNameNormalized` itself stays untouched — it stays Tier 1's (`tier1.ts`) exact-match key, Tier 2's (`tier2.ts`) fuzzy-match input, and `Subscription`'s grouping key, so categorization and subscription detection keep working exactly as before; the rename only changes what's displayed. Needs the schema field, a repository update method, and `PATCH` support — naturally the same endpoint CAT-12's edit popup calls, rather than a separate route. The edit control itself is CAT-12's popup, not a standalone UI.
- [ ] **CAT-14** — Create custom categories from the UI. The backend has been ready since CAT-9 — `CategoryRepository.create(userId, name, color)` — but CAT-9's own delivered note already flagged that nothing calls it: "a user can only get a richer list today by adding a custom category themselves via `CategoryRepository.create()`, since no route/UI calls it yet." Needs a `POST /api/categories` route (a color is required at creation — CAT-10's existing default-palette logic can seed a sensible one rather than forcing a color picker up front — plus an icon per CAT-11, once that field exists) and a "create new category" affordance, most naturally inline inside CAT-12's edit popup rather than a separate management page.

## Epic: XFER — Transfer Matching (Cross-Account Reconciliation)

- [x] **XFER-1** — Transfer-matching candidate logic (pure function): opposite-signed, same-magnitude, `TRANSFER_*` category, date-tolerance window (section 2.4, ADR-0006). _Reads `Transaction.providerCategory` (ADR-0029, a new field), not the CAT-9 taxonomy -- see that ADR for why they're different signals. Deterministic greedy pairing (`apps/worker/src/transfer/matching.ts`)._
- [x] **XFER-2** — `transfer-matching` BullMQ job: runs after categorization completes for a sync batch, scoped to the user's own accounts. _Chains off `categorize-llm`'s completion (`triggerTransferMatching()`, called from `categorizeLlm.ts`), unconditional on whether that run confirmed anything -- matching only needs `providerCategory`, already set at sync time._
- [x] **XFER-3** — Apply a match: set `transferGroupId` + `excludeFromCashFlow` on both sides. _`TransactionRepository.applyTransferMatch()` was already built and tested ahead of this story landing; the job just generates a `crypto.randomUUID()` per pair and calls it._
- [x] **XFER-4** — Age unmatched `TRANSFER_*` transactions into the Tier 4 review queue after a configurable window (`XFER_UNMATCHED_AGE_DAYS`, default 3 days). Preserves the existing category value, just flips `tier`/`status` to `needs_review` -- idempotent across runs. _The manual "confirm external / pair it" actions this feeds still have no UI to call them from -- CAT-7/`apps/web`, the same deferred gap ADR-0027/ADR-0028 already flag._
- [x] **XFER-5** — Emit the affected-date-bucket signal for rollup recompute (feeds ANLY-2). _`TransferMatchingResult.touchedDayBuckets`/`.touchedMonthBuckets`, same shape/mechanism `SyncConnectionResult` already uses -- left deliberately unwired for ANLY-2, same as provider-sync's own signal._
- [x] **XFER-6** — Unit tests for the matching heuristic — synthetic transaction pairs, edge cases (fees, same-day duplicates), written first. _`apps/worker/src/transfer/matching.test.ts`, hand-traced against the implementation (vitest can't run in either sandbox this was built in -- AGENTS.md)._

- [ ] **XFER-7** — Manual transfer review UI — the missing half XFER-4 already flagged ("the manual 'confirm external / pair it' actions this feeds still have no UI to call them from"). Context, since this is the feature the user found confusing: Plaid never links the two sides of a transfer between your own accounts, so the `transfer-matching` job (XFER-1/2/3) looks for opposite-signed, same-magnitude transaction pairs within a date-tolerance window where at least one side carries a `TRANSFER_*`/payment-type signal (`Transaction.providerCategory`, ADR-0029), and auto-links matches via `transferGroupId` + `excludeFromCashFlow: true` so they stop counting as both income and an expense (ARCHITECTURE.md §2.4). XFER-4 ages anything that looked like a transfer but found no match into the `needs_review` queue after 3 days — today that item is indistinguishable there from an ordinary miscategorized transaction, since `ReviewPage.tsx` shows the same plain category dropdown for it with no "is this actually a transfer?" framing. This story adds, for a `needs_review` transaction whose `providerCategory` is a transfer/payment-type signal, a choice between "confirm this is a real external transaction" (falls through to the ordinary category correction, CAT-7's existing path) and "link to another transaction" (auto-suggested candidates via the same opposite-sign/close-amount/nearby-date heuristic `findTransferMatches()` already implements, then `TransactionRepository.applyTransferMatch()` on the chosen pair). Two real gaps beyond frontend wiring: (1) `findTransferMatches()`/`isTransferSignalCategory()` (`apps/worker/src/transfer/matching.ts`) is pure and dependency-free but lives in `apps/worker`, which `apps/api` cannot import from (AGENTS.md's dependency direction — the same reason CAT-7's route didn't import `apps/worker`'s write-back helper) — it needs to move to `packages/shared` so both the worker's batch job and a new API suggestion route call the same logic instead of forking it; the read side already has a running start in `TransactionRepository.findUnmatchedTransferCandidates(userId)` (built for XFER-2's own pool), which returns exactly the pool a per-transaction suggestion query would search. (2) **Decided (2026-09-01, user confirmed)** — `TransactionRepository.applyTransferMatch(transactionIds, transferGroupId)` takes raw transaction ids with no `userId` scoping at all today, safe only because its one existing caller (the worker job) already operates on a user-scoped candidate pool. The new route must validate against the requesting session's `userId` (`req.session.userId`, `requireAuth`'s existing contract) before it's allowed to touch either transaction — the same `{_id, userId}` pattern `updateCategoryForUser()`/`updateColor()` already use elsewhere in this codebase. Concretely: add `userId` as a parameter and fold it into the `updateMany` filter itself (`{_id: {$in: transactionIds}, userId}`), so an id belonging to another user simply can't match; and _before_ calling it, verify a count of matching documents equals the expected pair size (2), rejecting the whole request (404) if it doesn't — otherwise `updateMany` would silently apply a partial, one-sided match when only one of the two ids actually belongs to the caller, leaving one transaction flagged as a linked transfer with no counterpart. Thread the same `userId` through the worker job's own call site too (it already has it on hand from `findUnmatchedTransferCandidates(userId)`), so this one change closes the gap for every caller, not just the new route.

## Epic: WEB — Frontend Application Foundation

`apps/web` today is exactly what SETUP-1 left it: a `package.json`, `tsconfig.json`, `eslint.config.js`, and `vitest.config.ts`, plus one placeholder `src/index.ts` -- no framework, no router, no pages. Three other backlog items are already waiting on this epic rather than each bootstrapping their own slice of it: AUTH-5 (login page), CAT-7 (Tier 4 review queue UI), and ANLY-9..11 (the actual dashboard pages). This epic is that one shared foundation, built once instead of three times.

- [x] **WEB-0** — Mock up the UI before writing any application code: 2-3 key screens (login, the dashboard shell/nav, one data-heavy page such as net worth or spending) as reviewable artboards via the `design` skill (Claude Design canvas), in a couple of candidate visual directions, so WEB-1/WEB-5's framework/token choices start from an agreed-on look instead of a guess. Not throwaway -- the direction picked here is what WEB-5 turns into real Tailwind/shadcn tokens. _Delivered as a single six-artboard canvas (login, dashboard overview, spending -- both directions): "Mockup A", grounded in the taste-skill's `minimalist-ui` + `design-taste-frontend-v1` sub-skills (warm monochrome, single terracotta accent, monospace figures for data; `design-taste-frontend` v2 was set aside -- it explicitly disclaims dashboards/product UI), against "Mockup B", shadcn/ui defaults on independent judgment with no taste-skill input (Inter, blue-600 primary, conventional shadcn chrome). Both share the same illustrative data and the same dataviz categorical palette. User picked Mockup A (2026-08-29); its palette/type/radii are now locked in as constants -- see WEB-5 and `apps/web/src/design/tokens.ts` (ADR-0030)._
- [x] **WEB-1** — Bootstrap `apps/web` as a Vite + React + TypeScript SPA (ADR-0011 already chose this over Next.js), with Tailwind CSS installed alongside it -- shadcn/ui (the chosen component foundation, WEB-5) is Tailwind + Radix primitives copied into the repo, not a normal npm UI-kit dependency, so Tailwind needs to be there from the start. Install the framework into the existing SETUP-1 stub, base `index.html`/entry point, real `dev`/`build` scripts replacing the current placeholder echoes. _Vite 8 + React 19 + Tailwind v4 (current stable majors, cross-checked against the npm registry and each other's peer ranges rather than assumed -- ADR-0031). Tailwind wired via `@tailwindcss/vite` + an `@config` directive pointing at `tailwind.config.ts`, which imports `src/design/tokens.ts` (ADR-0030) rather than re-typing values into CSS -- tokens.ts stays the one file WEB-5 (and anyone else) edits for a look change. `index.html`/`src/main.tsx`/`src/App.tsx` replace the SETUP-1 `src/index.ts` placeholder. **Not verified end-to-end**: `pnpm install` could not be run from this session's device shell (a Linux VM -- see ADR-0031's "trap" note); package.json's new deps were added by hand with registry-checked versions, so `tsc --noEmit` currently reports every new import as unresolved until you run `pnpm install` yourself and it's on you to confirm `pnpm --filter @financial-os/web dev` actually renders._
- [x] **WEB-2** — Client-side routing -- pick a router and lay out the route table for every page this backlog already names: login (AUTH-5), net worth, cash flow, spending categories, subscriptions, and the review queue (CAT-7). _React Router v7 (`react-router-dom`, ADR-0032), `src/router.tsx`. `/login` public; the other five nest under `ProtectedRoute` -> `AppShell`, each rendering a `PageStub` until ANLY-9..11/CAT-7 build the real page. `/` redirects to `/net-worth` -- no distinct overview route was ever named here, so none was invented. **Update (2026-08-31)**: confirmed live in a real browser -- the full nested tree (ProtectedRoute -> AppShell -> five page routes, plus the standalone /login) renders and routes correctly; see ANLY-9's same-day addendum for the full page-by-page pass._
- [x] **WEB-3** — API client + data-fetching layer: a typed fetch wrapper against `apps/api` with `credentials: 'include'` for AUTH-2's session cookie, a React Query provider, and shared 401 → redirect-to-login handling. ANLY-9's "React Query hooks" and ANLY-10's SSE-triggered cache invalidation both build on this rather than each rolling their own. _`src/api/client.ts`'s `apiFetch()` (ADR-0032): always `credentials: 'include'`, JSON in/out by default, throws a typed `ApiError` on any non-ok response instead of returning one. 401 handling is a `QueryCache` `onError` hook (`src/api/queryClient.ts`) that writes `null` into the one `['auth','me']` query key -- no event bus, no raw `window.location` redirect. Verified with real vitest-shaped unit tests against actual `Response` objects (`client.test.ts`), hand-traced rather than executed -- see WEB-6._
- [x] **WEB-4** — Auth-guarded application shell: session bootstrap via `GET /api/auth/me`, a redirect-if-unauthenticated route wrapper, and the base layout (nav/header). This is AUTH-5's actual implementation -- see the cross-reference on that story. _`src/auth/session.ts`'s `useSessionQuery()` is the one place `/api/auth/me` is called; `ProtectedRoute.tsx` reads it and redirects to `/login` (remembering `location` for the post-login bounce-back) or renders `<Outlet/>`. `AppShell.tsx` is the nav/header, limited to the five destinations WEB-2 names, plus a logout button (`useLogoutMutation`). `LoginPage.tsx` is AUTH-5's actual page. **Verified**: `ProtectedRoute.test.tsx` (RTL, WEB-6) covers both the authenticated-render and unauthenticated-redirect paths against a real `QueryClientProvider`+`MemoryRouter` tree. **Update (2026-08-31)**: both paths also confirmed live in a real browser, not just the RTL suite -- an authenticated session reaches every page, and clicking sign-out correctly destroys the session and redirects to /login._
- [x] **WEB-5** — Design system foundation: shadcn/ui (Radix primitives + Tailwind, chosen over a black-box npm component kit because it copies component source into the repo -- easy to restyle every component to match whatever WEB-0 picks) as the component foundation, with color/typography/spacing tokens configured in Tailwind to match WEB-0's chosen mockup direction. Reuses the `dataviz` skill's already-validated categorical palette (`references/palette.md` -- the same one CAT-10's category colors cycle through) as the color foundation rather than picking a second, inconsistent system, so chart colors and UI chrome read as one system. _Design-skill pick, decided when this epic was opened: WEB-0's mockup pass uses the `design` skill (Claude Design canvas, already available) -- lightweight, no install, matched to a solo self-hosted app with no separate design team. The `design` marketplace plugin (design-critique/design-system/accessibility-review/design-handoff skills, plus Figma/Notion/Slack MCP servers) was evaluated and set aside for now: its accessibility-review and design-system skills could be worth adding later for an ongoing audit/token-management workflow, but the plugin's bundled MCP servers assume a team/Figma-file workflow this project doesn't have. Separately, the user installed the open-source `taste-skill` (tasteskill.dev, project-local, installed via `npx skills add Leonxlnx/taste-skill` rather than through the account skill system) as extra design guidance; WEB-0's Mockup A drew on its `minimalist-ui` and `design-taste-frontend-v1` sub-skills, and that direction is the one now locked in._ **Palette/typography locked in (2026-08-29):** Mockup A's colors, fonts, and radii are centralized as constants in `apps/web/src/design/tokens.ts` (ADR-0030) so WEB-1's Tailwind setup imports them into `theme.extend` rather than duplicating hex values -- change the look by editing that one file. Category colors are deliberately excluded from that file: they're already a solved, per-user, editable field (`Category.color`, ADR-0028), not a frontend constant.
- [x] **WEB-6** — Component testing conventions for `apps/web`: React Testing Library wired into the existing `vitest.config.ts` stub (SETUP-5), extending AGENTS.md's test-first convention to the frontend's pure-enough logic (route guards, form validation) the same way it already applies to the backend's resolvers. _`src/test/setup.ts` registers `@testing-library/jest-dom/vitest`'s matchers, wired via `vitest.config.ts`'s `setupFiles` (jsdom environment was already configured by SETUP-5). Convention established against real code, not a throwaway example: `client.test.ts` (plain vitest, mocked `fetch`) and `ProtectedRoute.test.tsx` (RTL, mocked `fetch` + real `QueryClientProvider`/`MemoryRouter`). ADR-0033._

- [x] **WEB-7** — Connect Account page: the actual Plaid Link frontend flow. _Gap found 2026-08-31, same session as AUTH-6: ING-3's backend half (`POST /api/plaid/link-token`, `POST /api/plaid/exchange` -- `apps/api/src/routes/plaid.ts`) has been done and tested for a while, but nothing in `apps/web` ever calls it. `react-plaid-link` isn't even a dependency (checked `apps/web/package.json` -- it isn't there). Needs: the dependency added, a page that fetches a link token, opens Plaid Link, handles its exit/success callbacks, and POSTs the resulting public token to `/api/plaid/exchange`. **Open question, not decided here**: whether this lives behind a persistent nav item (an "Accounts" page, add more later) or only surfaces as an empty-state prompt on first login when zero accounts exist -- a product call, not an engineering one. **Scope addition (2026-08-31)**: the user wants to test the whole connect -> sync -> categorize -> dashboards loop against Plaid Sandbox before ever linking a real bank -- already the default (`PLAID_ENV=sandbox` in both `apps/api`/`apps/worker`'s env config and `.env.example`), nothing to change there. What WEB-7 should add: a visible "Sandbox mode" indicator (e.g. in `AppShell`'s header) whenever `PLAID_ENV !== "production"`, so mock data can never be mistaken for a real linked account later. Deployment-wide (from the existing global env var), not per-connection -- a per-connection version was considered and explicitly not chosen._ **Delivered (2026-08-31)**: `react-plaid-link@5.0.0` added to `apps/web/package.json` (registry-checked peer range `"react": "^16.8.0 || ^17 || ^18 || ^19"` against this app's real `react@^19.2.8`; exact `usePlaidLink` types confirmed against the published `.d.ts`, not assumed). New pure join function `apps/api/src/accounts/list.ts`'s `buildAccountList(accounts, connections)` -- an Account carries no status of its own, that lives on its parent Connection, so this joins and sorts by institution then subtype, defaulting an orphaned `connectionId` to `"error"` rather than throwing -- test-first, 7 hand-traced cases in `list.test.ts`. Two new `requireAuth`-gated routes registered in `server.ts`: `GET /api/accounts` (`routes/accounts.ts`, calls `buildAccountList()`) and `GET /api/config` (`routes/config.ts`, returns `{plaidEnv}` for the Sandbox badge below). Frontend: `api/accounts.ts` (`useAccountsQuery`/`useCreateLinkTokenMutation`/`useExchangePublicTokenMutation`, the last invalidating `["accounts"]` on success) and `api/config.ts` (`useConfigQuery`, `staleTime: Infinity`); `pages/AccountsPage.tsx` renders the linked-account list (balances, connection-status badge) plus a "Connect a bank" button wired through `usePlaidLink`, with loading/error/empty states. **Open question above, resolved**: a persistent `/accounts` nav item, first position in `AppShell`, not an empty-state-only prompt -- `findByUserId()` already returns arrays and a user linking a second account needs somewhere to come back to; full reasoning in ADR-0039 (this was decided in-session, not re-confirmed with the user first -- worth a look if the placement doesn't feel right). **Sandbox badge** (the scope addition above) sits next to `AppShell`'s wordmark, sourced from `useConfigQuery()`, shown whenever `plaidEnv !== "production"` -- deployment-wide, not per-connection, per the user's own "just a badge" choice. **Verified**: `apps/api`'s `tsc --noEmit` and `eslint .` both clean. `apps/web`'s `eslint .` clean; `tsc --noEmit` shows exactly 2 errors, both attributable solely to `react-plaid-link` not yet being installed in this session's sandboxed VM (`Cannot find module 'react-plaid-link'`, plus one cascading `noImplicitAny` on `onSuccess`'s parameter that contextual typing resolves automatically once the real package installs) -- same `pnpm install`-gap class as every `apps/web` dependency added this project (ADR-0031). **Unverified**: whether Plaid Link actually opens and a Sandbox institution can be linked end-to-end against a live backend -- needs the user's own `pnpm install` plus a real click-through, not yet done from this session._ **Update (2026-08-31, same day)**: `pnpm install` has since been run for real on the user's own Mac -- confirmed by finding `react-plaid-link@5.0.0` actually present in `apps/web/node_modules` from this session's device-bridge shell (which bind-mounts the same repo directory, not a separate copy). `apps/web`'s `tsc --noEmit` now reports zero errors, not the two expected placeholder errors -- the real install resolved both, as predicted. **Still unverified**: an actual live click-through (does Plaid Link open, does a Sandbox institution complete linking) -- that needs a browser, not just a clean typecheck._
- [x] **WEB-8** — Transactions list page: a general ledger view (merchant, amount, date, category, account) -- currently the only transaction-facing UI anywhere is the four analytics aggregates; there is no page that just shows a user's transactions. _Gap found 2026-08-31. Reusable backend already exists and is unused: `TransactionRepository.findByUserAndDateRange()` (`packages/db/src/repositories/TransactionRepository.ts`). **New backend work still needed**: no route exposes it yet (only `/api/analytics/*` exist) and the method itself has no pagination (`.find(...).sort(...)`, no `skip`/`limit`) -- fine for a bounded-range rollup, not fine for a raw list that could run to thousands of rows. Needs a `GET /api/transactions` route (paginated) plus a real transaction-row component (the one `design/tokens.ts`'s `TAG_PALETTE` doc comment already anticipated but nothing ever built)._ **Delivered (2026-08-31)**: not built on `findByUserAndDateRange()` after all -- that method requires a bounded date range, and a general ledger with no filters applied yet needs "give me the next N, most recent first," so a new method, `TransactionRepository.findPageForUser()`, was added instead (`page`/`pageSize` offset pagination, `pageSize + 1` fetched and sliced to compute `hasMore` rather than a separate `countDocuments()`). It also takes an optional `status` filter -- see this ticket's own text above naming `GET /api/transactions?status=needs_review` as CAT-7's mechanism, so that filter was built in now rather than left for CAT-7 to add a near-duplicate method for. New `GET /api/transactions` route (`routes/transactions.ts`, `requireAuth`-gated, registered in `server.ts`), backed by a pure join function `transactions/list.ts`'s `buildTransactionList()` (Transaction -> Account for institution/subtype, same denormalization pattern `accounts/list.ts` used for WEB-7, orphaned `accountId` falls back to a placeholder rather than throwing) -- test-first, 7 hand-traced cases in `list.test.ts`. Frontend: `api/transactions.ts` (`useTransactionsQuery`, `placeholderData: keepPreviousData` so the current page stays visible while the next loads), `components/TransactionsTable.tsx` (the row component this ticket's own text called out -- pulled out of the page specifically so CAT-7 can import it rather than re-copy it, with an unused `renderRowAction` slot reserved for CAT-7's category-correction control), `pages/TransactionsPage.tsx` (Previous/Next pagination, 50 rows per page). Added to `router.tsx` as `/transactions` and to `AppShell`'s nav, second position (after Accounts, ahead of the four analytics pages) -- it's the raw ledger those four are aggregates _of_. Full reasoning in ADR-0040. **Verified**: `apps/api`'s `tsc --noEmit`/`eslint .` and `apps/web`'s `tsc --noEmit`/`eslint .` all clean -- genuinely clean this time, not the "2 expected errors" caveat WEB-7 shipped with, since `pnpm install` has since actually run on the user's Mac (confirmed by finding `react-plaid-link` really present in `node_modules` from this session's device-bridge shell). **Unverified**: real end-to-end behavior against a live Mongo -- does the pagination actually feel right with real data volume, do pending transactions/orphaned accounts ever really occur -- `vitest` still cannot run from this sandboxed session (`@rollup/rollup-linux-arm64-gnu` missing, the same platform-mismatch class as every prior attempt), so `list.test.ts` is hand-traced only, and the page itself hasn't been clicked through in a real browser._

- [ ] **WEB-9** — Color-code transaction amounts: income green, expenses red. `TransactionsTable.tsx` renders every amount identically today (`formatCents(item.amount)`, no conditional styling). Easy to get backwards: this codebase's sign convention (documented in `matching.ts`'s own doc comment and relied on by ANLY-5's `amount > 0` spending filter) is Plaid's, not the intuitive one — **positive `amount` = money leaving the account (an expense)**, **negative = money in (income/a credit)** — so red is `amount > 0`, green is `amount < 0`, not the reverse. Worth deciding alongside WEB-11 (the redesign pass) rather than before it, so the red/green pair actually matches whatever palette that lands on — `design/tokens.ts` has `text-critical-text` for red-ish text already (today's query-error states); nothing analogous exists yet for a positive/success green.
- [ ] **WEB-10** — Transactions list filters: a specific date range, a "this calendar month" shortcut, and (added 2026-09-01) a category filter — ANLY-14's Spending drill-down depends on this last one, so land it before ANLY-14. `TransactionsPage.tsx` only supports offset pagination today (`useTransactionsQuery({page, pageSize})`) — no date or category filter exists anywhere in `apps/web`, and `TransactionRepository.findPageForUser()` takes neither parameter (only `page`/`pageSize`/`status`). Needs a backend half (extend `findPageForUser()`'s options and the `GET /api/transactions` query schema with `dateFrom`/`dateTo`/`category`, mirroring how the `ANLY` endpoints already take a `range`) and a frontend half — reuse `components/DateRangeControls.tsx`, already built for the analytics pages (ANLY-9), for the date part, plus a category `<select>` (populated from `useCategoriesQuery()`, already used by `ReviewCategoryControl.tsx`) for the new part.
- [ ] **WEB-11** — UI redesign pass: the user's own read is that the shipped app "feels very AI-generated" and "boring." Not a from-scratch redesign — WEB-0/WEB-5 already made a deliberate, non-default visual choice (the `taste-skill`'s `minimalist-ui` + `design-taste-frontend-v1` sub-skills: warm monochrome, single terracotta accent, monospace figures, locked into `apps/web/src/design/tokens.ts`, ADR-0030) over shadcn's out-of-the-box defaults. Worth a discussion before picking a direction: the same project-local `taste-skill` pack (`.claude/skills/`, installed via `npx skills add Leonxlnx/taste-skill`) has several other style sub-skills that were never applied — `industrial-brutalist-ui` (rigid grids, Swiss-print-meets-terminal, utilitarian), `high-end-visual-design`/`stitch-design-taste`/`gpt-taste` (agency-tier motion and layout, more animation/asymmetry-heavy than a data-dense dashboard usually wants), and — probably the most directly relevant one — `redesign-existing-projects`, whose entire job is auditing an already-built interface for generic/default-feeling patterns and fixing them in place rather than starting over. **Sequencing worth flagging**: several stories above add real UI surface (`ACCT-1`'s rename control, `CAT-11`'s icon picker, `XFER-7`'s transfer-pairing UI, `WEB-10`'s filters) — deciding this direction first means they get built once in the new look instead of built plain and redone.

## Epic: ANLY — Analytics & Dashboard

- [x] **ANLY-1** — Rollup job: `DailyBalanceSnapshot` + `MonthlyRollup` via `$merge`, triggered post-sync and by transfer-matching (section 4.4). _Implemented as a targeted Mongoose upsert per bucket, not a literal `$merge` pipeline -- ADR-0038's last bullet explains why (AGENTS.md's test-first convention needed a separable pure function). Pure computation in `apps/worker/src/rollups/recompute.ts` (`computeDailyBalanceSnapshot()`/`computeMonthlyRollup()`, test-first, `recompute.test.ts`, 15 tests hand-traced); `apps/worker/src/queues/rollups.ts`'s `runRollup()` is the queue glue. Historical balance reconstruction confirmed with the user before writing any of it -- ADR-0034._
- [x] **ANLY-2** — Targeted, bucket-scoped recompute shared by the sync job and the transfer-matching job (ADR-0008). _`triggerRollups(userId, dayBuckets, monthBuckets)` (`apps/worker/src/queues/rollups.ts`), called independently from both `providerSync.ts`'s and `transferMatching.ts`'s job handlers with each job's own touched-bucket set (XFER-5's already-built signal, finally wired up) rather than chaining one off the other -- ADR-0034 explains why a single trigger from the last stage would miss most transactions. No dedup `jobId` on this queue, unlike the other three -- recompute is idempotent, so overlapping jobs are harmless and deduping would risk silently losing buckets (ADR-0034)._
- [x] **ANLY-3** — Net Worth API endpoint: reads `DailyBalanceSnapshot`, handles partial-history gap rendering. _`GET /api/analytics/net-worth` (`apps/api/src/routes/analytics.ts`), backed by `apps/api/src/analytics/netWorth.ts`'s pure `fillNetWorthSeries()` (carry-forward gap-fill, test-first, `netWorth.test.ts`, 7 tests) -- ADR-0035. Response grew a 5th `accounts[]` field beyond section 4.2's original sketch, feeding ANLY-11's per-day coverage gap._
- [x] **ANLY-4** — Monthly Cash Flow API endpoint: `excludeFromCashFlow` filter applied. _`GET /api/analytics/cash-flow`, backed by `cashFlow.ts`'s pure `fillMonthlyCashFlowSeries()` (zero-fill, not carry-forward -- ADR-0035 explains why that's the correct semantics for a summed period rather than a point-in-time value; test-first, `cashFlow.test.ts`, 4 tests) reading `RollupRepository.getMonthlyRollup()`, itself built on `MonthlyRollup` rows the rollup job (ANLY-1) writes only from non-transfer, non-removed transactions._
- [x] **ANLY-5** — Categorical Spending Distribution API endpoint (on-demand aggregation). _`GET /api/analytics/spending-categories`, backed by `TransactionRepository.getCategoryDistribution()` -- a `$group`/`$sort`/`$project` aggregation against `Transaction`, filtered to `amount > 0` (Plaid spending convention) and `excludeFromCashFlow: {$ne: true}` (ADR-0035). Response has no color field -- see ADR-0038 for how `SpendingCategoriesChart` (ANLY-9) handles that on the frontend._
- [x] **ANLY-6** — Comparison-range (`$facet`) support: `range` + `compareRange` params on relevant endpoints. _`compareStart`/`compareEnd` on all three read endpoints (shared `rangeQuerySchema`, `apps/api/src/routes/analytics.ts`), required as a pair or not at all. `$facet` used only for spending-categories (`getCategoryDistributionComparison()`, against the large raw `Transaction` collection, matching section 4.2's own worked example) -- cash flow's comparison instead calls `RollupRepository.getMonthlyRollup()` twice against the small pre-aggregated `MonthlyRollup` collection, deliberately not `$facet` there (ADR-0035). Both endpoints return a unified `{current, compare}` shape, `compare: null` when no comparison was requested._
- [x] **ANLY-7** — Subscription-detection heuristic: nightly batch job, interval clustering, writes to `Subscriptions`. _`apps/worker/src/subscriptions/detect.ts`'s pure `detectSubscriptions()` (test-first, `detect.test.ts`, 10 tests): ≥3 occurrences, ±15% amount tolerance of the group's median, population stddev of intervals ≤5 days, mean interval within ±5 days of 30 or ±20 days of 365 -- no invented "other" bucket. Nightly cron (`SUBSCRIPTION_DETECTION_CRON`, default 3am UTC) via BullMQ's `upsertJobScheduler`, over every user (new `UserRepository.findAllIds()`), with lapse reconciliation (`SubscriptionRepository.markInactive()`). New `SubscriptionRepository` (`upsertDetected`/`findActiveByUser`/`markInactive`, 6 integration tests). ADR-0036._
- [x] **ANLY-8** — SSE `/events` route (`apps/api`): BullMQ `QueueEvents` listener, `sync.completed` / `transfer.matched` events (section 4.3, ADR-0007). _Three listeners, not two -- `rollup.completed` added as the most direct "dashboard numbers changed" signal (ADR-0037). `events/publisher.ts` (Fastify-free pub/sub) + `routes/events.ts` (`reply.hijack()`, per-user filtered, 30s heartbeat). Dedicated third Redis connection (`getEventsConnection()`) since `QueueEvents` blocks on Redis Streams reads, same reasoning as `apps/worker`'s per-Worker connections._
- [x] **ANLY-9** — Frontend feature folders: net-worth, cash-flow, spending-categories, subscriptions, review-queue (React Query hooks + Recharts/Chart.js components). _Delivered as 4 pages, not 5, and as hand-rolled SVG, not Recharts/Chart.js -- both deviations explained in ADR-0038 (review-queue is CAT-7's scope, not ANLY-9's, per WEB-2's own delivery note; no charting library was ever added as a dependency and this session cannot `pnpm install` one). `api/analytics.ts` (typed `apiFetch` + React Query hooks in one file, matching `auth/session.ts`'s existing convention), `pages/{NetWorth,CashFlow,Spending,Subscriptions}Page.tsx`, `components/charts/{NetWorthChart,CashFlowChart,SpendingCategoriesChart}.tsx` (built to the `dataviz` skill's method: the validated palette from `design/tokens.ts`, hover tooltips, `<details>` table-view disclosures), `components/DateRangeControls.tsx`, `lib/{dateRange,money}.ts` (`dateRange.ts` pure and test-first, `dateRange.test.ts`, 6 tests). Wired into `router.tsx` in place of 4 of the 5 `PageStub`s -- `/review` untouched. **Unverified**: same `pnpm install`-gap class as the rest of `apps/web` -- `tsc --noEmit`/`eslint` clean, hand-traced, not run through `vitest`/a real browser._ _Addendum (2026-08-31): this session's first real `docker compose up --build` run surfaced one genuine bug -- `vite.config.ts`'s `server` block never set `host`, so Vite's dev server bound to the `web` container's own loopback only. That left the process alive but unreachable from anywhere else, including the `proxy` container's `web:5173` reverse-proxy target -- surfaced as a 502 from Caddy, with the only hint being `web`'s own log line ("Network: use --host to expose") since the healthcheck stub above didn't catch it. Fixed with `server: { host: true }`; `tsc --noEmit` clean on the change. The stack is now confirmed reachable end-to-end at `https://localhost` (Caddy's self-signed local cert, expected). Page-by-page click-through (does each of the 4 real dashboard pages + login actually render against a live backend, any console/network errors) is the next thing to confirm, not yet done as of this addendum._ **Update, same day**: done, via the Chrome extension browser tools against the real `pnpm dev` stack. All 5 routes (login, net worth, cash flow, spending, subscriptions) plus the CAT-7 `/review` stub render correctly with zero console errors; every expected API call (`/api/auth/me`, all 4 `/api/analytics/*` endpoints, `/events` SSE) returned 200. Cash flow's "Compare to previous period" toggle exercised live -- `previousPeriodRange()` produced the correct `compareStart`/`compareEnd` query params and a second stat card rendered. Empty states (no accounts/transactions linked yet on this fresh account) render sensibly on every page rather than erroring. **Not exercised**: ANLY-11's dashed partial-coverage segments specifically, since that needs multiple accounts linked at different times to trigger -- nothing to show without real Plaid data linked.
- [x] **ANLY-10** — SSE client (`apps/web`): invalidate relevant React Query caches on event receipt, background-refetch fallback. _`lib/useDashboardEvents.ts`, mounted once in `AppShell` (not per-page): one `EventSource` for the whole authenticated app, listening for all three named events and invalidating the whole `["analytics"]` query-key prefix on any of them (coarse by design -- ADR-0038). Background-refetch fallback: every analytics hook in `api/analytics.ts` also sets `refetchInterval: 5 minutes` (paused while the tab isn't visible), matching section 4.3's "keep React Query's background refetch on as a fallback in case the SSE connection drops."_
- [x] **ANLY-11** — Net-worth chart gap-rendering for accounts with partial backfill history. _`components/charts/netWorthGaps.ts`'s pure `flagPartialCoverage()`/`toSegments()` (test-first, `netWorthGaps.test.ts`, 8 tests hand-traced) -- flags a day `isPartial` when its `accounts.length` is below the series-wide max, splits the series into solid/dashed `<path>` runs sharing their boundary point. `NetWorthChart` renders partial runs dashed at reduced opacity, and the hover tooltip calls out "Not all accounts linked yet" on those days._
- [x] **ANLY-12** — Integration tests for aggregation pipelines (`$dateTrunc`, `$merge`, `$facet`) via `mongodb-memory-server`. _Real `mongodb-memory-server` integration tests exist for every repository method this epic added: `RollupRepository.test.ts` (incl. `getLatestNetWorthSnapshotBefore`, an ObjectId round-trip check), `TransactionRepository.test.ts` (`getCategoryDistribution`, `getCategoryDistributionComparison`'s `$facet`, `findAccountDeltasAfter`, `findForCashFlow`, `findSubscriptionCandidates` -- ~25 tests), `SubscriptionRepository.test.ts` (6 tests). **Scope note, not silently glossed over**: this codebase's rollup writes don't use `$dateTrunc` or `$merge` anywhere (confirmed by search, not assumption -- ADR-0038's last bullet explains the substitution: Mongoose upserts of pre-computed JS values instead), so there's nothing of that shape to integration-test; `$facet` is the one named stage that's real here and it's covered. **Unverified**: same class as every other `mongodb-memory-server` suite in this repo -- cannot execute `vitest` from this environment (confirmed again this window: `Cannot find module '@rollup/rollup-linux-arm64-gnu'`), so these are hand-traced against Mongoose/mongodb-memory-server's documented behavior, not run.

- [ ] **ANLY-13** — Redesign the Net Worth page into the app's overview/home page. **Decided (2026-09-01, user confirmed)**: renames to "Overview" (nav label + route, e.g. `/overview` -- `/` already redirects to this route per WEB-2, so that part needs no change) and absorbs Cash Flow -- the separate Cash Flow page and nav item go away. Shows a compact stat row (Total Balance, Total Period Income, Total Period Expenses) plus one balance graph with a Days/Weeks/Months granularity toggle, replacing the current Assets/Liabilities/Net-worth stat row (`NetWorthPage.tsx`). `ANLY-3`'s `DailyBalanceSnapshot` already computes real net worth (assets minus liabilities, not a raw balance sum -- credit/loan balances are subtracted, per `computeDailyBalanceSnapshot()`'s own `liabilities`/`assets` split) -- recommend keeping that correct math under the friendlier "Total Balance" label rather than regressing to a naive sum of raw balances. "Total Period Income"/"Total Period Expenses" are Cash Flow's numbers (`ANLY-4`, monthly-bucketed) moving onto this page. **Still open**: Cash Flow's own month-by-month bar chart (`CashFlowChart.tsx`, real working code from ANLY-9) -- fold it into this page as a secondary section below the stat row/graph, or drop it now that its headline numbers live in the stat row? Leaning toward keeping it as a secondary section rather than discarding working code, but flagging rather than deciding unilaterally. The Days/Weeks/Months toggle is new: `NetWorthChart.tsx` today is daily-granularity-only over whatever custom range `DateRangeControls` picks, no bucketing option -- doable as a client-side downsample of the existing daily series (last value per week/month) rather than a new backend aggregation, since `DailyBalanceSnapshot` already has full daily resolution within any requested range. Touches `router.tsx` (route rename, Cash Flow route removal) and `AppShell.tsx`'s nav (`NAV_ITEMS`, relabel + remove the Cash Flow entry).
- [ ] **ANLY-14** — Click a category on the Spending page to see its transactions. `SpendingCategoriesChart.tsx`'s rows are plain `<div>`s today, not links -- needs an `onClick`/`<Link>` per row navigating to `/transactions` with that category and the Spending page's currently-selected date range applied as query params, plus `TransactionsPage.tsx` reading those params on mount to pre-apply the filter. Depends on `WEB-10` landing its category filter first -- there's nothing to link to otherwise. The "Other" folded row (categories beyond `CATEGORICAL_PALETTE`'s 8 slots, `SpendingCategoriesChart.tsx`'s `buildRows()`) has no single category to filter by -- either exclude it from the click affordance, or pass all of its folded category names as a multi-value filter, which `WEB-10`'s filter would then need to support.

## Epic: SEC — Security & Self-Hosting (deferred-but-not-skippable items)

- [ ] **SEC-1** — Field-level encryption for sensitive fields (account/routing numbers, raw payloads) — CSFLE or application-level AES-GCM.
- [ ] **SEC-2** — Scheduled `mongodump` backup to encrypted, off-box storage.
- [ ] **SEC-3** — Confirm Plaid webhook JWT signature verification is in place (cross-check against ING-7). _Implemented in ING-7; still needs an end-to-end check against a real Plaid-signed webhook, since the JWK path can only be exercised against live Plaid._
- [ ] **SEC-4** — Docker network posture audit: confirm Mongo/Redis have no host-exposed ports, only the reverse proxy does.
- [ ] **SEC-5** — Secrets audit: confirm no credentials are baked into images, `.env` is git-excluded, and production uses Docker secrets or a mounted file.

---

## Explicitly Out of Scope (Phase 1)

- Budgeting math (burn-rate, allowance logic) — deferred per `ARCHITECTURE.md` section 0.
- FX conversion / multi-currency support — ADR-0001, revisit only if a foreign-currency account is linked.
- Full historical backfill (>30 days) — ADR-0002, planned as an explicit user-triggered action later, not a Phase 1 story.
