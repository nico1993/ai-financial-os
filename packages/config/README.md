# @financial-os/config

Home for the shared `tsconfig.base.json`, ESLint config, and Prettier config
referenced in `ARCHITECTURE.md` sections 7.3/7.4. Populated by SETUP-2,
SETUP-3, and SETUP-4 — empty by design until then.
