# Personal Financial OS — Architecture

**Status:** Phase 1 (architecture finalized). Phase 2 (implementation) not yet started.

## 0. Scope

Personal Financial OS is a self-hosted, automated personal finance engine: it ingests bank data via Plaid and provides analytics/dashboards. It is a solo project — one user, not a product being built for multiple tenants — so architectural choices favor simplicity and low operational overhead over scalability.

**In scope for this phase:** data ingestion (Plaid sync, categorization, transfer reconciliation) and analytics/dashboards.

**Explicitly out of scope for this phase:** budgeting math (burn-rate, allowance logic).

---

## 1. Executive Summary

The core architectural bet is to treat this system as an **event-sourced ledger with a derived read model**, not a simple CRUD app over a `transactions` table. Plaid's `/transactions/sync` endpoint is itself a change-stream (added/modified/removed), so the ingestion layer is built around that abstraction from day one rather than bolted on later.

Five decisions anchor the ingestion/data side; the tech stack (section 7) anchors everything else:

1. **BullMQ + Redis as the sync orchestrator**, with one queue for provider webhooks/polling and a separate queue for categorization, so a slow LLM call never blocks transaction ingestion.
2. **A 4-tier categorization pipeline implemented as a single BullMQ flow (parent/child jobs)**, where each tier is a discrete job type that can be retried, monitored, and cost-audited independently.
3. **Raw payloads stored immutably, cleaned/normalized documents stored separately**, linked by a stable `providerTransactionId`. This gives replay-ability if categorization logic or schema changes later.
4. **MongoDB schemas denormalized for time-series read performance**, with compound indexes built around the actual dashboard queries (net worth over time, monthly cash flow, category breakdown) rather than a generic index-everything approach.
5. **Docker Compose as the deployment unit from the start**, with secrets, encryption keys, and Plaid credentials treated as first-class infrastructure concerns, not app config.

---

## 2. Data Ingestion Architecture

### 2.1 Provider Abstraction Layer

Plaid is the only provider for Phase 1, but the ingestion and categorization pipeline never call the Plaid SDK directly — they call a `FinancialProvider` interface, with `providers/plaid/PlaidProvider.ts` as the sole implementation for now:

```
interface FinancialProvider {
  createLinkToken(input): Promise<{ linkToken }>
  createConnection(publicToken): Promise<ConnectionResult>
  syncTransactions(connection, cursor): Promise<{ added, modified, removed, nextCursor, hasMore }>
  getAccounts(connection): Promise<NormalizedAccount[]>
  verifyWebhook(req): Promise<boolean>
  parseWebhook(body): ProviderWebhookEvent
}
```

`createLinkToken` isn't in the original sketch above — Plaid Link can't initialize client-side without a server-issued `link_token`, and the same "never call the Plaid SDK directly outside the adapter" rule that motivates this interface means that call has to live here too. It bakes in the 30-day backfill cap (ADR-0002, via `days_requested`) so callers don't have to know that constant exists. `verifyWebhook` is `Promise<boolean>`, not the synchronous `boolean` this section originally sketched — real JWK-based signature verification requires an async key fetch. See ADR-0017. `parseWebhook` is a later addition for the same reason (ADR-0024): the webhook receiver has to know whether a payload means "sync this connection", and reading Plaid's `webhook_code` in a route would put provider-specific naming right where this abstraction exists to keep it out.

The "Plaid Item" concept is renamed to the provider-neutral **`Connection`** everywhere in the app layer (jobs, routes, UI) — Plaid-specific naming (`itemId`, `plaid_error`) stays inside the adapter only. Intentionally lightweight for a solo project: no dynamic plugin loading, no attempt to support multiple providers simultaneously — just enough indirection that a second provider (a different aggregator, or a manual CSV-import "provider") means writing a new adapter, not touching the sync or categorization pipeline. See ADR-0004.

### 2.2 Plaid `/transactions/sync` + BullMQ/Redis

Plaid's sync endpoint is cursor-based: send a `cursor` (empty on first call), get back `added`, `modified`, `removed` arrays plus `has_more` and a `next_cursor`. The worker's job is to drain that cursor to completion on every trigger, not just fetch once.

Queue structure:

- **`provider-sync` queue** — one job per `connectionId` (a `Connection` roughly maps to one linked bank login; today that's always a Plaid Item under the hood). Triggered by (a) provider webhooks (Plaid's `SYNC_UPDATES_AVAILABLE`), and (b) a scheduled BullMQ repeatable job as a fallback safety net (e.g., every 4–6 hours) in case a webhook is missed.
- **Job body** contains only `{ connectionId }` — the job handler loads the current cursor from the `Connections` collection at execution time, not at enqueue time, and resolves the right `FinancialProvider` adapter from the connection's `provider` field. This avoids stale-cursor bugs if multiple sync jobs for the same connection get queued back to back.
- **Idempotency lock**: use a Redis lock (or BullMQ's built-in job-id deduplication, e.g. `jobId: `sync:${connectionId}`` with `removeOnComplete`) so a webhook retry and a scheduled poll for the same connection can't run concurrently and race on the cursor. **Correction:** that `jobId` example is not usable as written — BullMQ rejects any custom job id containing `:`, since colons are its own Redis key separator. The implementation uses `sync-{connectionId}`; see ADR-0022.
- **Pagination loop inside the job**: while `has_more` is true, keep calling sync with the returned `next_cursor`, persisting the cursor to the DB after each page (not just at the end) so a crash mid-pagination resumes cleanly rather than reprocessing from scratch.
- **Rate limits**: Plaid enforces per-Item and per-client rate limits. Use BullMQ's `limiter` option on the queue (e.g., max N jobs per second) rather than ad hoc `setTimeout` throttling, and treat `429`s as retryable with exponential backoff (BullMQ's built-in `attempts` + `backoff` job options).
- **On completion**, the sync job enqueues categorization jobs (one per new/modified transaction, or batched) into a separate queue — keeping ingestion throughput decoupled from categorization latency.

### 2.3 The 4-Tier Categorization Pipeline

Model this as a **BullMQ Flow**: a parent "categorize transaction" job with a chain of child job types, where each tier either resolves the category (short-circuiting later tiers) or falls through.

- **Tier 1 — Exact Match (static).** A synchronous lookup against a `MerchantRules` collection keyed on Plaid's cleaned merchant name / `personal_finance_category` or a normalized merchant string. Runs **inline in the sync job itself**, not as a separate queued job — it's a cheap DB read and doesn't need queue overhead. Maybe 60–80% of recurring transactions (subscriptions, known payroll, known utilities) resolve here instantly.
- **Tier 2 — Regex/Fuzzy (user-defined).** User-authored rules (e.g., "if description matches `/UBER \*TRIP/i` → Transportation"). Also inline/synchronous — regex evaluation against a small rule list is fast. Store rules with a `priority` field so overlapping rules order deterministically. This is also where fuzzy matching (Levenshtein/trigram) applies against previously-categorized merchants manually corrected before, effectively "learning" from Tier 4 corrections.
- **Tier 3 — LLM Inference (async, queued).** Only transactions that fall through Tiers 1–2 get queued into a `categorize-llm` queue. Batch these (e.g., 20–50 transactions per call) using structured JSON output (function calling / JSON schema mode) to minimize per-call overhead and cost. This queue has its own concurrency limit independent of the provider-sync queue, since the LLM backend has its own rate/throughput profile. Cache LLM decisions back into the Tier 1 exact-match table keyed on the normalized merchant string — this is the mechanism by which Tier 3 usage should shrink over time as the exact-match table absorbs prior LLM decisions (CAT-6, ADR-0027). **Correction:** this section originally assumed OpenAI specifically ("per OpenAI call", "since OpenAI has its own rate limits"). The implementation is backend-neutral: a `CategorizationProvider` interface (mirroring §2.1's `FinancialProvider`) sits between this queue and whatever model answers it, with Ollama running a local model (`gpt-oss:20b` by default) as Phase 1's implementation rather than a hosted API — see ADR-0026.
- **Tier 4 — Human-in-the-loop.** Anything the LLM returns with low confidence (require a `confidence` field in the structured output) or explicitly flags as `uncertain`, plus anything manually recategorized, lands in a `needs_review` status on the transaction. This is a dashboard queue, not a background job — it's UI-driven. A manual correction here writes back into the Tier 1/Tier 2 rule tables, closing the loop so the same merchant never needs LLM inference again. **Correction:** the write-back mechanism itself (`MerchantRuleRepository.upsertExact()`) is built and wired for the LLM half (ADR-0027) — this dashboard/UI and the manual-correction call into it are CAT-7, still not built.

The efficiency argument for separating these into distinct tiers isn't just cost (LLM calls are the expensive path) — it's also that Tiers 1–2 are deterministic and auditable, which matters for a financial system where "why was this categorized as X" needs a concrete answer.

### 2.4 Transfer Matching (Cross-Account Reconciliation)

Plaid does not link the two sides of a transfer between your own accounts — there's no `transaction_id` relationship connecting, say, a "Transfer to Savings" debit in checking with the corresponding credit in savings, or a credit-card payment leaving checking with the payment landing on the card. All Plaid gives you is a signal, via `personal_finance_category` (`TRANSFER_IN` / `TRANSFER_OUT`, or detailed values like `TRANSFER_OUT_ACCOUNT_TRANSFER`, `LOAN_PAYMENTS_CREDIT_CARD_PAYMENT`) — it flags "this is probably a transfer," not who its counterpart is.

A **transfer-matching pass** runs as a queued job after categorization completes for a sync batch:

- Scope the search to the user's own accounts (never cross-user).
- Candidate pairs: opposite-signed transactions with equal (or near-equal, to allow for a fee) magnitude, where at least one side has a `TRANSFER_*` or payment-type category, and dates fall within a small tolerance window (a few days — ACH transfers commonly settle 1–3 days apart across accounts).
- On a match, link both sides via a shared `transferGroupId` and set `excludeFromCashFlow: true` on both — this is the critical part, since an unflagged internal transfer otherwise counts as both income and an expense and inflates every cash-flow chart in section 4.
- Unmatched `TRANSFER_*`-categorized transactions age into the Tier 4 review queue after a short window (e.g., 3 days with no match found), rather than sitting silently unresolved — confirm it's an external transfer (leave as-is) or manually pair it.
- This pass emits the same "affected date range" signal used by the sync job (see section 4.4) so any rollup that already included the now-reclassified transactions gets recomputed.

**Correction:** implemented in XFER-1..XFER-6 as a `transfer-matching` BullMQ job chained off `categorize-llm`'s completion (ADR-0029). The "TRANSFER_*/payment-type category" signal above is `Transaction.providerCategory` -- a raw Plaid field added in ADR-0029 -- not the CAT-9 taxonomy `category.value` a transaction is ultimately categorized into; the two never shared a naming scheme.

See ADR-0006, ADR-0029.

---

## 3. Data Modeling & Performance

### 3.1 Raw vs. Cleaned Data

Store two collections per data type: `RawPlaidTransactions` (or a generic `RawPayloads` collection with a `source`/`type` discriminator) holding the untouched Plaid response, and `Transactions` holding the normalized, app-facing document. Link them via `providerTransactionId` (Plaid's `transaction_id`), not by MongoDB `_id`.

Reasons this matters more here than in a typical CRUD app:

- **Auditability** — if a categorization is disputed or a balance looks wrong, show exactly what the bank sent.
- **Replay-ability** — if categorization logic, normalization rules, or schema change later, reprocess `RawPlaidTransactions` into a new `Transactions` collection without re-hitting Plaid's API (which has both rate limits and a cost per Item).
- **Plaid data corrections** — Plaid itself sometimes retroactively modifies a `pending` transaction into a `posted` one with a different `transaction_id` (pending→posted linkage via `pending_transaction_id`). Keeping the raw record lets you reconcile this cleanly rather than ending up with duplicate cleaned transactions.

Raw payloads are a simple insert-only collection (no updates), which keeps write patterns cheap. Cleaned `Transactions` documents are the ones indexes and queries are built around.

### 3.2 Schema Design for Time-Series Aggregation

```
Connection {          // was "Item" — provider-neutral connection record
  _id
  userId
  provider          // 'plaid' (only value today; future-proofs for a 2nd provider)
  providerItemId     // Plaid's item_id, opaque to the rest of the app
  institutionName
  status             // 'active' | 'login_required' | 'error'
  cursor             // provider sync cursor, lives here not on a separate table
  lastSyncedAt
}

Account {
  _id
  userId
  connectionId        // ref -> Connection
  provider
  providerAccountId    // Plaid's account_id
  institutionName
  type          // depository, credit, loan, investment
  subtype
  officialName
  currentBalance
  availableBalance
  isoCurrencyCode
}

Transaction {
  _id
  userId
  accountId          // ref -> Account
  providerTransactionId   // Plaid transaction_id, unique index
  pendingTransactionId    // for pending->posted reconciliation
  date                // posted date, stored as UTC Date, NOT string
  authorizedDate
  amount              // stored as integer cents, never float
  isoCurrencyCode
  merchantName        // Plaid's cleaned name
  merchantNameNormalized  // normalized/lowercased key for Tier 1 lookups
  description          // raw description, for fuzzy/regex matching
  category: {
    tier: 1 | 2 | 3 | 4,
    value: String,       // e.g. "Groceries"
    confidence: Number,  // for LLM tier
    status: 'confirmed' | 'needs_review'
  }
  transferGroupId      // set by the transfer-matching pass (section 2.4); links both sides
  excludeFromCashFlow  // true once matched as an internal transfer; filtered out of income/expense rollups
  pending: Boolean
  isRemoved: Boolean     // soft-delete flag for Plaid "removed" events
  createdAt, updatedAt
}
```

Key modeling decisions: amounts as **integer cents**, not floats or Decimal128 unless sub-cent precision is specifically needed (it isn't, for a personal finance tool) — avoids floating point drift in aggregation pipelines. Dates as native `Date` objects in UTC, with timezone conversion happening at the presentation layer, so `$dateTrunc`/`$group` by month work correctly in aggregation without string parsing.

**Indexes** to support the dashboard's actual query patterns:

- `{ userId: 1, date: -1 }` — the workhorse index; almost every dashboard query filters by user and sorts/ranges by date.
- `{ userId: 1, 'category.value': 1, date: -1 }` — for categorical spending distribution and category drill-downs.
- `{ providerTransactionId: 1 }` unique — for upsert-on-sync idempotency (this is what makes Plaid's added/modified/removed events safe to apply as upserts).
- `{ accountId: 1, date: -1 }` — for per-account views and net worth reconciliation.
- `{ userId: 1, 'category.status': 1 }` — for the Tier 4 review queue.
- `{ transferGroupId: 1 }` sparse — for looking up both sides of a matched transfer.

For net worth and monthly cash flow, don't recompute from raw transactions on every dashboard load — use MongoDB's `$merge`/scheduled aggregation into a pre-computed `DailyBalanceSnapshot` or `MonthlyRollup` collection (populated by a nightly or post-sync job), and have the dashboard read from that. Write-time aggregation for anything read on every page load; on-demand aggregation only for ad hoc/drill-down queries.

### 3.3 Persistence Layer / Database Abstraction

Mongo was chosen specifically for its aggregation pipeline (`$dateTrunc`, `$merge`, compound indexes) and denormalized document shape — that choice doesn't disappear behind an abstraction, and a genuinely backend-agnostic query layer is over-engineering for a one-user system. What's worth doing: put every persistence operation behind a **repository per aggregate** (`ConnectionRepository`, `AccountRepository`, `TransactionRepository`, `RollupRepository`, `RawPayloadRepository` — plus `UserRepository` for ADR-0018's auth), with domain-shaped method names — `upsertFromSync()`, `findByUserAndDateRange()`, `getMonthlyRollup(userId, range)` — rather than exposing the native driver or generic CRUD to routes and job handlers. This keeps intent readable regardless of backing store, and means a future relational migration is contained to rewriting repository internals and rollup jobs rather than a rewrite scattered through the whole app — it won't make such a migration painless (aggregation-pipeline logic and document shape are still Mongo-specific), but it bounds the blast radius. See ADR-0005.

---

## 4. Analytics & Visualization Strategy

### 4.1 Frontend Structure (Vite + React SPA + TS + Shadcn)

Organize the "Financial Control Center" around **query-scoped panels**, not a monolithic dashboard component, inside the `apps/web` Vite SPA (see section 7 for the full repo layout):

```
/apps/web/src
  /routes                (React Router route components — one per dashboard page)
    net-worth.tsx
    cash-flow.tsx
    spending-categories.tsx
    subscriptions.tsx
    review-queue.tsx
  /features
    /net-worth        (NetWorthChart, useNetWorthQuery)
    /cash-flow         (CashFlowChart, useCashFlowQuery)
    /spending-categories (CategoryDonut, useCategoryBreakdown)
    /subscriptions      (SubscriptionTable, useRecurringQuery)
    /review-queue       (Tier4ReviewList)
  /components/ui        (shadcn primitives)
  /lib/api              (typed fetch client, one function per dashboard endpoint, hitting apps/api)
  /lib/events            (SSE client, section 4.3)
```

Each feature owns its own data hook (React Query / TanStack Query — caching, background refetch on sync completion, and loading/error states for free, which matters when the backing data updates asynchronously via BullMQ jobs the frontend didn't trigger). Use Shadcn's `Card`, `Tabs`, and `DataTable` primitives as the layout scaffolding rather than custom-building dashboard chrome. Since it's a private single-user dashboard behind auth, there's no need for SSR/SSG — a plain client-rendered SPA is simpler and there's nothing to gain from server rendering here.

### 4.2 Serving Data to Recharts/Chart.js

The general principle: **the API returns chart-ready data, not raw transactions for the frontend to reduce.** Push aggregation into MongoDB's pipeline, not into the browser.

- **Net Worth (aggregated):** endpoint reads from the pre-computed `DailyBalanceSnapshot` collection, returns `[{ date, netWorth, assets, liabilities }]`. Never touches the raw `Transactions` collection at request time. For an account with limited backfill history (see 30-day backfill in section 6), the series starts at that account's first snapshot date rather than implying a $0 balance before it existed — the frontend renders a visible gap/dashed segment for the period before an account was linked, not a flat line.
- **Monthly Cash Flow (income vs. expense):** an aggregation pipeline grouping `Transactions` by `$dateTrunc` month and `amount > 0 / < 0` (or a dedicated `direction` field), filtering out `excludeFromCashFlow: true` (transfers matched by the section 2.4 pass), returning `[{ month, income, expenses }]`. If hit often, back it with the same rollup-on-write pattern as net worth.
- **Categorical Spending Distribution:** `$group` by `category.value` within a date range, `$sum` amount, returned pre-sorted for the donut/bar chart. Cheap enough to compute on-demand given the compound category index above.
- **Recurring Subscription Tracking:** requires detecting *recurrence*, not just aggregating. Phase 1 heuristic: group by `merchantNameNormalized`, flag as recurring any merchant with ≥3 transactions where the amount is within a small tolerance band and the interval between dates clusters around ~30 or ~365 days (simple standard-deviation-on-interval check). Runs as a nightly batch job writing to a `Subscriptions` collection rather than a live query. Plaid also offers a native Recurring Transactions endpoint; worth evaluating as a swap-in for the DIY heuristic once Phase 1 is stable.
- **Comparison views (MoM/YoY, custom ranges):** dashboard endpoints accept a `range` and an optional `compareRange`, computed in a single `$facet` aggregation rather than two round trips, returning `{ current: [...], compare: [...] }`.

### 4.3 Live Dashboard Updates

The backing data changes asynchronously — a sync or categorization job can finish seconds or minutes after the page loaded. Use **Server-Sent Events**, not WebSockets: the traffic is one-directional (server → client, "something changed, go refetch"), so SSE avoids the reconnect/bidirectional complexity a WebSocket would add for no benefit here. A BullMQ `QueueEvents` listener inside `apps/api` publishes lightweight events (e.g., `{ type: 'sync.completed', connectionId }`, `{ type: 'transfer.matched' }`) to a `/events` SSE route (Fastify supports streaming responses natively); the frontend's SSE client calls `queryClient.invalidateQueries()` for the relevant feature on receipt. Keep React Query's background refetch interval on as a fallback in case the SSE connection drops. See ADR-0007.

### 4.4 Rollup Recompute Strategy

`DailyBalanceSnapshot`/`MonthlyRollup` are write-time aggregates, which means they can go stale: a Plaid `removed` event, a late-arriving `modified` transaction, or the transfer-matching pass reclassifying a transaction as `excludeFromCashFlow` can all invalidate a rollup already computed. Rather than incremental delta math (fragile, especially for removals) or a full recompute on every change (wasteful), each job that mutates transactions — the sync job and the transfer-matching job alike — tracks which date buckets it touched during that run and enqueues a **targeted recompute** for just those buckets, using `$merge` to overwrite the affected documents. This bounds recompute cost to what actually changed and gives both triggers (provider sync, transfer matching) a single shared recompute path. See ADR-0008.

---

## 5. Privacy & Self-Hosting

Since this holds real bank data, the following are non-negotiable from the start rather than "harden later":

- **Docker Compose topology**: separate containers for the web app (static Vite build, served as static files — no Node runtime needed for it), the API (Fastify), the BullMQ worker(s), MongoDB, Redis, and a reverse proxy (Caddy/Traefik) handling TLS termination and routing to the web/API containers. Workers and API are separately scalable/restartable — a stuck LLM categorization job should never require restarting the API.
- **Secrets management**: Plaid `client_id`/`secret`, OpenAI API key, and Mongo credentials come from an `.env` file excluded from git, or better, Docker secrets / a mounted secrets file, never baked into the image.
- **Field-level encryption for sensitive fields**: consider encrypting account/routing numbers and raw payloads at rest (MongoDB Client-Side Field Level Encryption, or application-level AES-GCM before write) even though this is self-hosted — a stolen disk image shouldn't leak bank credentials in plaintext.
- **Network posture**: bind Mongo and Redis to the Docker internal network only, never exposed to the host's public interface. Only the reverse proxy has an external port.
- **Backups**: since this is the system of record for financial history, a scheduled `mongodump` to encrypted, off-box storage is not optional — losing the raw Plaid history means losing the ability to ever reconcile discrepancies.
- **Plaid webhook verification**: verify webhook JWT signatures (Plaid signs webhooks) rather than trusting the payload, since the webhook endpoint is internet-reachable.
- **Authentication**: session-based login (`User` collection, bcrypt-hashed passwords, Redis-backed session cookie) gates every route except `/api/auth/*` and the Plaid webhook receiver (which verifies its own JWT instead). Chosen over a shared API token or reverse-proxy Basic Auth because more than one person may eventually have their own login — a shared secret has no way to identify or revoke one person's access without rotating it for everyone. See ADR-0018.

---

## 6. Bottlenecks & Edge Cases (Living Register)

This section stays a living register with resolution status per item, updated as issues are actually handled — not a write-once doc.

- **Duplicate transactions from pending→posted transitions — resolved in ING-9.** Plaid sends a `pending` transaction, then later a new `posted` transaction with `pending_transaction_id` referencing the original. Handling this incorrectly is the single most common source of double-counted spending in Plaid integrations — the sync handler must treat the pending record as superseded (soft-delete or link, never leave both as active) rather than inserting both as live transactions. The sync job soft-removes the pending record when its posted counterpart lands, records *both* date buckets for recompute (a transaction authorized Friday and settled Monday was already counted in the earlier bucket), and carries any real category across — otherwise a manual Tier 4 correction made while the transaction was pending is silently discarded the moment it settles. A posted transaction whose account can't be resolved does *not* supersede its pending counterpart, since that would delete the only surviving record of the spend.
- **Plaid rate limits and Item error states — resolved in ING-6 and ING-10.** Items can enter `ITEM_LOGIN_REQUIRED` (re-auth via Plaid Link update mode needed) — the sync worker detects this error code, marks the account as needing re-auth, and stops retrying that Item until the user acts, rather than burning retry budget indefinitely. Two distinct terminal states are tracked, because the recoveries differ: `login_required` (re-auth via Link update mode) and `error` (access revoked — the connection has to be created again). Both are raised as `UnrecoverableError`, which stops BullMQ's remaining attempts immediately rather than backing off against an Item that cannot succeed, and both drop the connection out of ING-8's poll, which only sweeps `active` connections. Rate limits are handled separately (ADR-0023): a 429 pauses the whole queue and does not consume an attempt.
- **Cursor drift / `PLAID_ERROR` on stale cursors — resolved in ING-11.** If a cursor becomes invalid (e.g., after a long outage or Item re-link), Plaid returns an error requiring a full resync from an empty cursor — the worker needs an explicit "reset and full resync" path, not just retry-with-backoff. Two failure modes are deliberately kept apart: an *invalid cursor* clears the stored cursor and resyncs from empty (once — a second invalid-cursor failure propagates rather than looping), while a *mutation during pagination* restarts the drain from the cursor the run began with and leaves the stored cursor untouched. Conflating them would throw away a perfectly good cursor and force a needless full resync every time the bank posted something mid-drain. Restarts are bounded, so a provider stuck in that state fails the job to BullMQ's backoff instead of spinning the worker, and each attempt's counters are local so an abandoned pass isn't summed into the result.
- **LLM non-determinism and cost creep.** Without the Tier 1 write-back loop, LLM usage doesn't shrink over time, and categorization can be non-deterministic across runs for the same merchant. Structured JSON output plus low temperature helps, but the real fix is aggressive caching of LLM decisions into Tier 1.
- **Multi-currency accounts — descoped for MVP (ADR-0001).** Single currency only for this iteration; no FX conversion logic, and no per-account currency validation at link time. `baseCurrency` is a fixed env var, `isoCurrencyCode` stays on the `Transaction`/`Account` schema for forward compatibility, but it's treated as a constant, not something the aggregation pipeline needs to reason about yet. Revisit (including whether to validate/exclude mismatched accounts) if a foreign-currency account is ever actually linked.
- **Timezone boundaries on "monthly" aggregation — mostly resolved by consistent UTC, with one wrinkle.** Storing and aggregating everything in UTC eliminates the original concern (different parts of the pipeline disagreeing on what "month" a transaction falls in). The remaining gotcha is display: Plaid's `date` field is a calendar date with no time-of-day component — it represents "the day the bank says this posted," not a moment in time. If that date string gets parsed into a JS `Date` and rendered through the browser's local timezone, a UTC-midnight timestamp can flip back to the previous day for anyone west of UTC. So: aggregate in UTC (solved), but treat the field as a literal date string on display, not something to round-trip through timezone conversion.
- **Backfill volume on initial Item link — capped at 1 month for MVP (ADR-0002).** Rather than pulling Plaid's full available history (up to 24 months), request only ~30 days via `days_requested` at Link initialization. Keeps the first sync fast and day-one LLM categorization cost low. Full historical backfill can be added later as an explicit, user-triggered action.
- **Soft-delete propagation — resolved via targeted rollup recompute (section 4.4, ADR-0008).** Plaid's `removed` array means a transaction should disappear from the app — the sync job includes the affected date bucket(s) in its recompute signal, so any rollup collection that already included the removed transaction gets recomputed for just that bucket, not just the source record deleted.
- **Cross-account transfers aren't linked by Plaid — resolved via the transfer-matching pass (section 2.4, ADR-0006).** Without it, a transfer between your own accounts (e.g., checking → savings, or a credit-card payment) shows up as both an expense and income, inflating cash-flow charts. The matching pass links both sides via `transferGroupId`, marks them `excludeFromCashFlow`, and ages unmatched `TRANSFER_*`-categorized transactions into Tier 4 review after a few days rather than leaving them silently unresolved.

---

## 7. Tech Stack & Repository Structure

### 7.1 Monorepo: pnpm workspaces + Turborepo

One repo, not one package. pnpm workspaces handle dependency linking between apps and packages; Turborepo handles cached, parallelized task running (`turbo run lint test build`) across them. Chosen over Nx specifically because Nx's generator/plugin model is more machinery than a solo project needs — pnpm+Turborepo gets the caching and workspace-linking benefits with a fraction of the config surface. See ADR-0010.

```
/apps
  /web        — Vite + React SPA: dashboard UI only, no server-side rendering, talks to apps/api over HTTP + SSE
  /api        — Fastify: all HTTP-facing routes (dashboard endpoints, Plaid webhook receiver, SSE stream)
  /worker     — plain Node/TS process: BullMQ queues (provider-sync, categorize-llm, transfer-matching, rollups)
/packages
  /db         — Mongoose schemas + the repository layer (section 3.3): Connection/Account/Transaction/Rollup/RawPayload/User repositories
  /providers  — FinancialProvider interface (section 2.1) + PlaidProvider adapter
  /shared     — shared TS types/constants used by all three apps (Transaction, Account, Connection shapes, category enums)
  /config     — shared tsconfig.base.json, eslint config, prettier config
```

### 7.2 Vite SPA for the frontend, Fastify for the API, plain Node for the worker

Three apps, three distinct jobs, all plain TypeScript with no shared "do everything" framework. `apps/web` is a Vite-built React SPA — client-rendered only, since there's no SEO or anonymous-traffic benefit to gain from SSR on a private, single-user, authenticated dashboard. `apps/api` is a Fastify server owning every HTTP-facing route: the per-widget dashboard endpoints from section 4.2, the Plaid webhook receiver, and the SSE stream from section 4.3. `apps/worker` is a framework-free Node/TS process running the BullMQ queues, independent of any request lifecycle (queue workers need to run continuously regardless of framework, which is why section 5 calls for the API and the worker(s) to be separately restartable/scalable Docker containers). All three import the same `db` and `providers` packages, so schema, repository, and provider-adapter logic is written once and shared.

Fastify was chosen over Hono for the API layer specifically because this is a long-running self-hosted Node process, not an edge/serverless deployment — Fastify has the more mature plugin ecosystem and first-class TypeScript + JSON-schema/Zod validation support for that deployment model. See ADR-0011, ADR-0014.

`apps/api` (and later `apps/worker`) import `packages/db` and `packages/providers` as workspace packages, run directly from TypeScript source via `tsx` — no per-package build/`dist` step. `pnpm dev` and the Docker `CMD` both run `tsx`; `packages/*/package.json`'s `"build"` script stays a placeholder until a real production build (bundling, `tsc`-emitted `dist`) is actually needed. See ADR-0019.

### 7.3 TypeScript

`strict: true` plus `noUncheckedIndexedAccess`, defined once in `packages/config/tsconfig.base.json` and extended by every app/package — one place to tighten rules later, and no workspace silently opting out of strictness.

### 7.4 Linting & formatting: ESLint + Prettier, enforced from day one

`typescript-eslint` as the base, plus `eslint-plugin-react` + `eslint-plugin-react-hooks` for `apps/web` (React-specific correctness rules — hooks-of-hooks, dependency arrays), and a plain Node/TS ruleset for `apps/api` and `apps/worker`. Prettier handles formatting, wired to a shared config in `packages/config`. Husky + lint-staged run lint and format on every commit; a GitHub Actions workflow runs lint + typecheck + test on every push, even solo — cheap insurance against the class of mistakes that otherwise surface at 11pm. See ADR-0012.

### 7.5 TDD: Vitest, with the seam drawn at "pure logic vs. queue glue"

Vitest as the test runner across every package (fast, native ESM/TS, works identically across `apps/web`, `apps/api`, and `apps/worker`) over Jest. The parts of this system worth testing hard aren't "does BullMQ retry correctly" — that's the library's job — they're the pure business logic: Tier 1/2 categorization resolution, the transfer-matching candidate logic, the subscription-recurrence heuristic, the rollup bucket-tracking logic. The practical consequence: job handlers stay thin (queue glue only) and hand off to plain, framework-free functions that do the actual work — that's what makes TDD practical here. Two testing layers:

- **Unit tests (Vitest)** — the pure logic above, plus React component tests (Testing Library) for the handful of components with real conditional logic (e.g., the net-worth chart's gap-rendering for partial history).
- **Integration tests (Vitest + `mongodb-memory-server`)** — the repository layer and the aggregation pipelines specifically (`$dateTrunc`, `$merge`, `$facet`). These have real Mongo semantics a mock won't catch, so they get actual database-backed tests rather than unit tests with a stubbed driver.

See ADR-0013.

---

## 8. Decision Log (ADR-numbered)

This section is the project's ADR record — no separate `docs/decisions/` files. Each entry captures what was decided, what alternatives were considered, and why, so "future you" doesn't have to reconstruct the reasoning later.

- **ADR-0001 — Single-currency MVP.** No FX conversion, no per-account currency validation at link time. `baseCurrency` is a fixed env var. Full detail in section 6.
- **ADR-0002 — 30-day backfill cap on initial account link.** Full detail in section 6.
- **ADR-0003 — Serverful, not serverless.** Evaluated moving ingestion/categorization to a serverless stack (Lambda/SQS, Vercel + Upstash, etc.). Decision: stay serverful (BullMQ + Redis + Docker Compose). Rationale: single-user personal tool, not a product being sold — the operational simplicity of a self-hosted, always-running stack outweighs any cost/scaling benefit serverless would offer, and serverless would hand more of the "self-hosted/private" story to third-party managed services (Atlas, Upstash, cloud functions), cutting against the privacy goal.
- **ADR-0004 — Provider abstraction adopted from day one.** All Plaid access goes through a `FinancialProvider` interface (section 2.1); the app layer works in terms of a provider-neutral `Connection`, not a Plaid Item. Rationale: renaming/abstracting later means touching schema fields and every query that references them — doing it before any code exists is nearly free.
- **ADR-0005 — DB abstraction scoped to a repository layer, not a backend-agnostic query layer.** Mongo's aggregation pipeline and denormalized schema are core to the design and won't be hidden behind an abstraction; a repository-per-aggregate layer (section 3.3) isolates persistence calls and bounds the blast radius of a hypothetical future relational migration without pretending to make it painless.
- **ADR-0006 — Transfer matching built in Phase 1, not deferred.** A post-categorization matching pass (section 2.4) links both sides of a cross-account transfer and excludes them from cash-flow aggregation. Rationale: directly affects the correctness of Phase 1 analytics — an unmatched transfer silently inflates income/expense charts.
- **ADR-0007 — Live dashboard updates via SSE, not WebSockets.** One-directional data flow (server → client "something finished, refetch"), so SSE for lower complexity, backed by a BullMQ `QueueEvents` listener.
- **ADR-0008 — Rollup staleness resolved via targeted, bucket-scoped recompute.** Rather than incremental delta updates or full recomputes, any job that mutates transactions tracks which date buckets it touched and triggers a `$merge`-based recompute scoped to just those buckets. Also resolves the "soft-delete propagation" edge case.
- **ADR-0009 — Currency validation at link time was proposed, then explicitly rejected.** Considered checking each linked account's `isoCurrencyCode` against a base currency and soft-excluding mismatches from aggregates. Decision: not now — stay with ADR-0001. Revisit only if a foreign-currency account is actually linked.
- **ADR-0010 — Monorepo: pnpm workspaces + Turborepo.** Chosen over Nx for lower config overhead on a solo project.
- **ADR-0011 — Vite SPA (`apps/web`) + Fastify API (`apps/api`), not unified Next.js.** Next.js's core strengths (SSR/RSC, SEO, caching for anonymous traffic) don't apply to a private single-user dashboard, so keeping it would mean carrying real framework complexity for unused features.
- **ADR-0012 — ESLint + Prettier, not Biome.** Chosen for ecosystem maturity, particularly React-specific correctness rules for `apps/web`.
- **ADR-0013 — Vitest for TDD, not Jest.** Fast, native ESM/TS, consistent across all three apps.
- **ADR-0014 — Fastify over Hono for `apps/api`.** Long-running self-hosted Node process, not an edge/serverless deployment — Fastify's plugin ecosystem and validation tooling fit that model better.
- **ADR-0015 — Caddy over Traefik for the reverse proxy.** Section 5 left this as an either/or; SETUP-9 needed a concrete choice. Decision: Caddy — automatic HTTPS via Let's Encrypt with effectively zero config, and a Caddyfile that's dramatically simpler than Traefik's label-based or file-provider routing config, with no extra moving pieces (no separate cert-manager/ACME container) to operate. Traefik's core strength — dynamic service discovery as containers come and go — matters more for a multi-service platform with frequent deploys than for a solo, fixed-topology self-hosted stack. See `proxy/Caddyfile`.
- **ADR-0016 — `Connection.accessToken` added to the §3.2 schema.** The original Connection sketch (section 3.2) didn't include a field for Plaid's `access_token`, but `FinancialProvider.syncTransactions()` (section 2.1) can't call Plaid without one — it has to live somewhere, and `Connection` is the only per-Item record. Decision: store it as `accessToken` on `Connection`, `select: false` on the Mongoose schema so it's excluded from every query by default; `ConnectionRepository.findByIdWithAccessToken()` is the sole read path, used only by the provider-sync job. This is a bearer credential, not account data, but sensitive for the same reason section 5 calls out account/routing numbers — it should be in scope for SEC-1's field-level encryption before this ever holds a real token, not just app-level `select: false`.
- **ADR-0017 — `FinancialProvider.createLinkToken()` added to the §2.1 interface.** The original interface sketch (section 2.1) started at `createConnection`, implicitly assuming a `public_token` already exists — but AGENTS.md requires every Plaid call, including Link initialization, to go through this interface, and Plaid Link needs a server-issued `link_token` before the frontend can even open. Decision: add `createLinkToken(input: { userId }): Promise<{ linkToken }>` to the interface, implemented by `PlaidProvider` via `linkTokenCreate` with `transactions.days_requested` fixed at 30 (ADR-0002) so the cap can't be bypassed by a caller. Also formalizes `verifyWebhook` as `Promise<boolean>` rather than the sketch's synchronous `boolean`, since JWK-based webhook signature verification (section 5) requires an async key fetch.
- **ADR-0018 — Session-based authentication, not a shared token or reverse-proxy Basic Auth.** Section 4/5 called this "a private single-user dashboard behind auth" without ever deciding what that means, and `ING-3`'s Link routes need a real "current user" per request to move forward. Considered a single shared `API_TOKEN` bearer secret (simplest, but doesn't scale past one identity — no way to tell two people apart or revoke one without rotating it for both) and reverse-proxy Basic Auth (keeps the app itself auth-free, but same one-identity limit and no real login/logout UX). Decision: a `User` collection (`packages/db`) with bcrypt-hashed passwords, `packages/api` sessions backed by Redis (`@fastify/session` + `@fastify/cookie`, `connect-redis` as the store — Redis is already in the stack for BullMQ) — a `requireAuth` preHandler hook gates every route except `/api/auth/*` and the Plaid webhook receiver. `POST /api/auth/register` only succeeds when zero users exist yet (bootstrap) or the caller is already authenticated, so a second person can be added later without opening public signup. This is real work the "one shared secret" options aren't — a new `AUTH` epic (`BACKLOG.md`) covers it — but it's the only option that doesn't need redoing if a second login is ever wanted, which was the deciding factor over the lighter options.
- **ADR-0019 — Workspace packages run from source via `tsx`, no per-package build step.** `apps/api` needing to actually import `packages/db`/`packages/providers` at runtime (rather than just typecheck against them) surfaced a gap: neither package's `package.json` declared a `main`/`exports` entry, and there was no build pipeline wiring `tsc`'s `outDir: dist` output into what a consumer would resolve. Considered adding a real per-package build step (`tsc` to `dist`, referenced via project references or Turborepo pipeline ordering) — more correct for a package meant to be published or consumed by unrelated projects, but this monorepo has neither: every consumer is another workspace in the same repo, built and run together. Decision: give `packages/db`/`packages/providers` an `"exports"` field pointing straight at `src/index.ts`, and run `apps/api` (via `pnpm dev`/the Docker `CMD`) with `tsx`, which transpiles TypeScript/ESM on the fly, including across workspace symlinks. Matches ADR-0003's operational-simplicity bias; revisit if a package is ever published standalone or a real production bundle/minification pass is wanted.
- **ADR-0020 — `Transaction.category` is app-owned, written with `$setOnInsert` on sync.** Building the sync job (ING-4) surfaced a conflict the schema didn't resolve: `category` is required on every `Transaction`, so the sync job must supply one, but the sync job also re-upserts a transaction on every Plaid `modified` event (an amount correction, a pending→posted flip). With a plain `$set`, each of those would reset a transaction that Tier 1/2/3 had already categorized — or that the user had just manually corrected in the Tier 4 review queue — back to the sync job's placeholder. Decision: `category` is the one field on this document the app owns rather than the provider. `TransactionRepository.upsertFromSync()` applies it via `$setOnInsert` so it lands only on first write; `updateCategory()` is the sole path for changing it afterward. The sync job's placeholder is `{ tier: 4, value: "Uncategorized", status: "needs_review" }` — an honest description of an un-categorized transaction that routes it into the review queue (§2.3) and is claimed automatically once CAT-3/CAT-4 land, rather than a fake "confirmed" category.
- **ADR-0021 — `RawPayloadRepository` added to the §3.3 repository set, insert-only.** §3.1 requires the untouched provider response to be stored alongside the cleaned `Transaction`, and DATA-4 built the schema, but DATA-9 named only four repositories (Connection/Account/Transaction/Rollup) — leaving nothing able to write the collection §3.1 depends on. Decision: add `RawPayloadRepository` with `insert()` and `findByProviderId()` and no update or delete method, so the append-only property §3.1's auditability and replay-ability guarantees rest on is enforced by the repository's surface rather than by convention. The sync job writes the raw payload *before* normalizing, so a transaction rejected downstream (e.g. an unresolvable account) is still recoverable and replayable.
- **ADR-0022 — Provider-sync idempotency via BullMQ `jobId` dedup, not a Redis mutex.** §2.2 offered either; ING-5 needed a concrete choice. Decision: a per-connection `jobId` of `sync-{connectionId}`, set by the single shared `enqueueProviderSync()` helper so no trigger can skip it. (§2.2 sketches this id with a colon; BullMQ rejects that outright — `Custom Id cannot contain :` — because colons are its Redis key separator, so the separator is a hyphen. The first end-to-end run caught this, after unit tests had asserted the broken value and typecheck had no opinion.) A Redis mutex was rejected as redundant: BullMQ already guarantees one worker per job, and — more to the point — every write in the drain is idempotent by construction (transactions upsert on `providerTransactionId`, the cursor is persisted per page, duplicate `RawPayload` rows are explicitly fine per §3.1). The lock therefore exists to avoid wasted API calls and cursor thrash, not to prevent corruption, which makes the cheaper mechanism sufficient. **The trap this hides:** BullMQ resolves `jobId` collisions against jobs it still retains, *including completed and failed ones* — so `removeOnComplete`/`removeOnFail` must both be `true`, or the very first sync for a connection would permanently block every later one. History comes from logs and `Connection.status` (ING-10) instead of BullMQ's retained sets. **Known limitation, accepted:** a trigger arriving while a drain for that connection is already running is swallowed rather than queued behind it. The drain runs to `hasMore: false`, so it already collects anything the provider held when it started; only data landing after its final page waits, and ING-8's scheduled poll bounds that wait. If webhook-to-dashboard latency ever matters more than it does now, the fix is a coalescing flag (re-enqueue once at completion if a trigger arrived mid-run), not a switch to mutexes.
- **ADR-0023 — Provider failures are translated into a neutral error taxonomy at the adapter boundary.** The sync job must treat "slow down" (429), "this connection needs re-auth" (ING-10), and "your cursor is stale" (ING-11) as three different situations, but AGENTS.md keeps Plaid specifics inside the adapter — so the worker cannot switch on `error_code` or reach into an axios response without violating that boundary. Decision: `packages/providers/src/errors.ts` defines a `ProviderError` hierarchy that every adapter maps its own failures onto (`ProviderRateLimitError` first, carrying `Retry-After`); the mapping itself is a pure function (`mapPlaidError`) so it can be unit-tested against hand-built error shapes without the SDK. Unrecognized errors are rethrown **unchanged** rather than wrapped, preserving their stack and message — the taxonomy exists to make specific cases actionable, not to flatten everything into a generic wrapper. Members are added when a story consumes them, not speculatively.
- **ADR-0024 — `FinancialProvider.parseWebhook()` added to the §2.1 interface.** ING-7's receiver has to decide whether an inbound webhook means "sync this connection" or "nothing to do", which in raw form means reading Plaid's `webhook_type`/`webhook_code` — exactly the provider-specific naming AGENTS.md and ADR-0004 keep inside the adapter. Decision: add `parseWebhook(body: unknown): ProviderWebhookEvent`, reducing a verified body to a neutral discriminated union (`sync_updates_available` today; ING-10 adds `item_error`). Total by contract — malformed or unrecognized bodies return `ignored` with a human-readable reason rather than throwing — because the input arrives from the public internet, where a bad body is an ordinary case and an exception would just become a 500. The implementation (`parsePlaidWebhook`) is pure, so the mapping is unit-tested without an HTTP server. **Related routing decision:** the receiver answers `200` for a verified-but-unactionable webhook (unknown code, or an Item we hold no `Connection` for) and `401` only for failed verification. Plaid retries non-2xx responses, so returning an error for something that will never succeed just buys a retry storm.
- **ADR-0025 — The scheduled fallback poll gets its own queue, not a repeatable job on `provider-sync`.** §2.2 describes the safety-net poll as "a scheduled BullMQ repeatable job" without saying where it lives, and ING-8 needed a concrete answer. Putting it on `provider-sync` would mean that queue carried two unrelated job shapes — a per-connection drain keyed `sync:{connectionId}`, and a connectionless fan-out — forcing the payload into a union and the handler into a branch, and letting a slow drain block the sweep that schedules more drains. Decision: a fifth queue, `provider-sync-scheduler`, whose only job lists syncable connections and enqueues normal `provider-sync` jobs through ING-5's shared helper — so a poll landing on a connection a webhook already queued collapses onto the same job rather than racing it. The schedule is registered with `upsertJobScheduler` under a fixed id, making worker restarts idempotent instead of stacking duplicate schedules. The poll's work list comes from the database at run time, not from the schedule, so a newly linked connection is picked up without re-registering anything.

- **ADR-0026 — `CategorizationProvider` abstracts Tier 3's LLM backend; Ollama running `gpt-oss:20b` is Phase 1's implementation, not OpenAI.** §2.3's original sketch assumed OpenAI outright without ever deciding it the way ADR-0004 decided Plaid — CAT-4 needed a concrete answer to build the categorize-llm queue for real, and testing a local Ollama model first (for cost and offline-development reasons) meant that answer couldn't just be "call OpenAI". `gpt-oss:20b` — a 20B-parameter open-weight model, quantized to run on consumer hardware (roughly a 16GB unified-memory floor, comfortable at 24GB+) — is capable enough for a closed-choice classification task like this, but its Ollama-specific structured JSON output has documented reliability gaps: real, dated GitHub issues (`ollama/ollama#11691`, opened Aug 2025 and still open at the time of this decision; `#14440`, opened and since closed Feb 2026) report malformed JSON, markdown-fence-wrapped responses, and "Harmony" reasoning-format text leaking into the payload for this exact model. Decision: introduce `CategorizationProvider` (`packages/providers/src/CategorizationProvider.ts`) as the same kind of boundary ADR-0004 draws around `FinancialProvider` — the categorize-llm job calls `categorizeBatch(candidates, categories): Promise<CategorizationResult[]>` and never an LLM SDK/HTTP client directly. `OllamaCategorizationProvider` (`packages/providers/src/ollama/`) is the only implementation for Phase 1, resolved via a plain singleton factory in `apps/worker/src/provider.ts` (`getCategorizationProvider()`, built from `OLLAMA_HOST`/`OLLAMA_MODEL`) — swapping models, or swapping to a hosted API later, means changing what that factory constructs, not touching the job handler. The interface absorbs the reliability gap directly rather than letting a malformed response become an uncaught exception mid-batch: `categorizeBatch()` is total by contract, the same pattern ADR-0024 uses for `parseWebhook()` — it always returns exactly one `CategorizationResult` per input candidate, synthesizing `{confidence: 0, uncertain: true}` for anything it can't parse or reconcile, so nothing upstream special-cases a missing or thrown result. Inside `OllamaCategorizationProvider`, a totally-unparseable response gets one retry before the whole batch falls back to uncertain; a response that parses but is malformed for a handful of individual candidates falls back per-entry in `reconcileCategorizationResults()` instead of retrying the whole batch over one bad entry. Two more decisions this story needed: (1) the category taxonomy handed to the model is `DEFAULT_CATEGORIES` (`apps/worker/src/categorize/categories.ts`), a plain editable string array owned by apps/worker rather than a DB enum or something baked into `CategorizationProvider` (which takes `categories: readonly string[]` and has no opinion on its contents) — every categorize-llm batch is a stateless call with no memory of prior ones, and without a fixed list the same merchant could fragment across "Dining"/"Restaurants"/"Food & Drink" between runs. (2) Tier 3 results are always tagged `tier: 3` (`categorize/tier3.ts`'s `resolveTier3Outcome()`), never the `tier: 4` `UNCATEGORIZED` sentinel (ADR-0020), even when the call fails outright — this keeps `isUncategorized()`'s "never attempted" meaning precise, and means a low-confidence or failed Tier 3 attempt stays eligible for the *next* categorize-llm run automatically, since `findNeedsReview()` filters on `category.status`, not `category.tier`. **Known limitation, accepted:** the categorize-llm job takes a `findNeedsReview()` snapshot, then may spend real wall-clock time on LLM calls before writing results back — a manual Tier 4 correction (CAT-7, not yet built) landing on a transaction in that window could otherwise be silently overwritten by a stale verdict. Given this project's bar elsewhere for exactly this kind of race (ADR-0020's `$setOnInsert`, CAT-3's `isUncategorized()` guard), the job re-reads each transaction via `findById()` immediately before its `updateCategory()` write and skips it if the status has moved off `needs_review` in the meantime, rather than leaving it as deferred risk. Ollama itself runs natively on the host, not as a `docker-compose.yml` service — local inference wants direct GPU/unified-memory access a Linux container on macOS can't give it well through Docker Desktop. `OLLAMA_HOST` defaults to `http://localhost:11434` for host-side `pnpm dev`; `docker-compose.yml` overrides it to `http://host.docker.internal:11434` for the worker container, the same way it already pins `MONGO_URI`/`REDIS_URL`. **Addendum, found running this against real Ollama sandbox data (not caught by the hand-traced unit tests, which never call Ollama):** every real `/api/chat` call returned a completely empty `message.content` -- real compute time (tens of seconds), HTTP 200, but nothing in the field this code reads, so 100% of results hit the `parsed === null` fallback on both the call and its retry. Root cause, confirmed via `ollama/ollama#11691` and its duplicate `#11867` (both open) plus `docs.ollama.com/capabilities/thinking`: gpt-oss cannot fully disable its reasoning trace (Ollama's Harmony format) the way other "thinking" models can, and combined with `format`'s structured-output constraint, that trace can consume Ollama's default 4096-token context window before the model ever reaches its final-answer channel -- indistinguishable from the outside from a hung or broken model, since generation genuinely happened. Fix in `callOllama()`: an explicit top-level `think: "low"` (documented gpt-oss-specific values are `low`/`medium`/`high`/`max`; this bounds the trace instead of eliminating it) and `options.num_ctx: 8192` (up from the 4096 default) to give a full `CATEGORIZE_LLM_BATCH_SIZE`-sized batch's prompt, schema, and trace room to finish before content is written. `think` is a gpt-oss-specific mitigation, not a universal one -- if `OLLAMA_MODEL` is ever swapped for a non-thinking model, revisit whether it still applies. This also resolved a naming confusion in testing: `categorizeBatch()`'s existing one-retry-on-unparseable-response logic (a plain `if`, not a loop) was calling `callOllama()` exactly twice with these two fixes both landing before content was ever produced either time, which read like an unexplained loop from the terminal output alone.

- **ADR-0027 — CAT-6's write-back loop: confirmed Tier 3 decisions are cached into `MerchantRules` (Tier 1).** §2.3 always described this ("Cache LLM decisions back into the Tier 1 exact-match table — this is the mechanism by which Tier 3 usage should shrink over time") but it wasn't wired up until now. `MerchantRuleRepository.upsertExact()` is the write side matching CAT-3's existing read side (`findExactByUser()`), upserting on the same `(userId, pattern)` unique partial index (`matchType: 'exact'`) Tier 1's lookup already relies on. `apps/worker/src/categorize/writeBack.ts`'s `buildMerchantRuleWriteBack()` is the pure decision (`categorizeLlm.ts` calls it after `resolveTier3Outcome()`, before the I/O): only a `confirmed` category writes back, never `needs_review` — a low-confidence or uncertain-flagged guess hasn't earned a permanent rule, and writing one back would let one shaky call mis-categorize every future transaction from that merchant rather than just the one it was actually about. On conflict, `upsertExact()` is last-write-wins on `category`/`source`: in the normal flow this rarely fires at all (a merchant with an existing Tier 1 rule resolves at Tier 1 during sync and never reaches Tier 3 to begin with), and the realistic case is two transactions for the same not-yet-ruled merchant both confirming within one categorize-llm run. `MerchantRuleSource`'s `"manual"` value (already present in the schema, unused until now) is deliberately still unwired — CAT-7's Tier 4 manual-correction UI is the only caller that would ever pass it, and CAT-7 hasn't been built (see §2.3's Tier 4 bullet); this ADR only closes the LLM half of the loop §2.3 promised, and documents the manual half as a known, deliberate gap rather than an oversight. `CategorizeLlmResult` gained a `merchantRulesWritten` counter, logged alongside the existing `confirmed`/`stillNeedsReview`/`skippedRace` counters, so a shrinking Tier 3 workload over time (the whole point of caching) is something the logs can actually show.

- **ADR-0028 — Per-user `Category` collection replaces the single shared `DEFAULT_CATEGORIES` array as Tier 3's input; categories carry a user-editable color (CAT-9, CAT-10).** Running CAT-4/5 against real Plaid sandbox data (not synthetic samples) surfaced two real gaps in the Phase 1 taxonomy: several transaction shapes (a CD deposit, a loan payment, an ambiguous employer-name credit) had no category that fit well — correctly landing in `needs_review` rather than being miscategorized, but pointing at a taxonomy that needed to grow — and every user was stuck sharing one hardcoded list with no way to add, rename, or hide a category. Decision: a new `Category` collection (`packages/db/src/models/Category.ts`) — `{userId, name, color, isDefault, archived}` — with `CategoryRepository` (`findActiveByUser()`, `seedDefaults()`, `create()`, `updateColor()`) as this story's persistence layer, mirroring `MerchantRuleRepository`'s shape. `apps/worker/src/categorize/userCategories.ts`'s `resolveUserCategoryNames()` is `categorizeLlm.ts`'s new category source (replacing a direct `DEFAULT_CATEGORIES` import), called once per run rather than once per batch since categories don't change mid-run. Seeding is lazy, not a signup hook: the first time a user has zero `Category` rows, `resolveUserCategoryNames()` seeds `DEFAULT_CATEGORY_SEEDS` (`apps/worker/src/categorize/categories.ts`, the old `DEFAULT_CATEGORIES` restructured to pair each name with a default color) via `seedDefaults()`, which only inserts names that don't already exist for that user — re-seeding can never reset a category the user already renamed, recolored, or archived. This needed no migration script for the one real user who existed before this story shipped; their next categorize-llm run seeds them exactly as if they'd just signed up. `DEFAULT_CATEGORIES` (the flat name list) stays exported unchanged from `categories.ts` specifically because `apps/worker/src/scripts/test-categorization.ts` (the CAT-4/5 standalone smoke test) deliberately has no DB connection to seed or read a real `Category` collection from. CAT-10's color requirement was folded into this same model rather than shipped as a separate migration later — one new collection, one migration, not two — with `DEFAULT_CATEGORY_SEEDS` cycling the dataviz skill's validated 8-hue categorical palette (`references/palette.md`) in fixed order for the 17 real categories (repeating past slot 8, since that palette is validated for ≤8 simultaneously-legended series, an acceptable tradeoff for a seed default the user can freely recolor) and giving `"Uncategorized"` the chart-chrome muted ink tone instead of a categorical hue — deliberately, since a vivid color would visually suggest it's a normal spending category rather than the model's "none of these fit" sentinel. **Scope cut, deliberate:** no `apps/api` route and no `apps/web` UI exist yet to call `create()`/`updateColor()` from outside the seeding path — both are written and unit-tested, ready to wire up, but that surface naturally belongs alongside CAT-7's review queue UI, and both need `apps/web` (BACKLOG.md's Suggested Sequencing defers all `apps/web` work to the ANLY epic). `Transaction.category.value` stays free text, unchanged — this is additive, no migration needed for existing transactions.

- **ADR-0029 — Transfer matching (XFER-1..XFER-6) reads a new `Transaction.providerCategory` field, not the CAT-9 taxonomy.** §2.4 describes candidate pairs needing "at least one side has a `TRANSFER_*` or payment-type category" — Plaid's own `personal_finance_category.detailed` naming (`TRANSFER_IN_ACCOUNT_TRANSFER`, `LOAN_PAYMENTS_CREDIT_CARD_PAYMENT`, confirmed against Plaid's published taxonomy CSV), not the app's curated 18-name `Category` taxonomy (ADR-0028) a transaction's `category.value` holds after Tier 1-4 resolve it — the two never shared a naming scheme and CAT never claimed to derive one from the other. `FinancialProvider`'s `NormalizedTransaction.providerCategory` (§2.1) already carried this signal from the adapter, with a comment anticipating exactly this use, but `sync/normalize.ts`'s `toTransactionInput()` dropped it before it ever reached `Transaction` — a real gap, the same kind CAT-9 found running against real data, just found here by tracing the pipeline instead. Decision: add `Transaction.providerCategory` (optional string, `packages/db/src/models/Transaction.ts`), wire it through `toTransactionInput()`, and treat it as a provider-owned field (`$set` on every resync, alongside `amount`/`date`/etc.) — never app-owned like `category`, since it's the provider's classification, not a decision this app ever makes. `apps/worker/src/transfer/matching.ts`'s `isTransferSignalCategory()` recognizes the `TRANSFER_` prefix (every `TRANSFER_IN_*`/`TRANSFER_OUT_*` detailed value) plus one explicit payment-type value, `LOAN_PAYMENTS_CREDIT_CARD_PAYMENT` — §2.4's own named example — and deliberately not the rest of Plaid's `LOAN_PAYMENTS_*` family (mortgage/car/student/personal loan payments), since those typically leave for a servicer this app has no linked account for, making a match a guess rather than a documented case; extend the set later if real sandbox data (same discovery pattern as ADR-0028) shows otherwise. Consequence: BACKLOG.md's "XFER needs CAT's category signals" line in Suggested Sequencing is imprecise — matching only needs `providerCategory`, set at sync time, not any Tier 1-4 outcome — but XFER-2 still chains off `categorize-llm`'s completion rather than `provider-sync`'s, to keep the documented pipeline order (provider-sync → categorize-llm → transfer-matching → rollups, `apps/worker/src/index.ts`'s own top comment) rather than two queues firing off the same sync event.

  **XFER-1's matching algorithm** (`transfer/matching.ts`, pure, test-first per AGENTS.md's TDD convention): deterministic greedy pairing. Every candidate carrying a transfer/payment signal ("anchor") is visited in a fixed `(date, id)` order; each searches the *whole* candidate pool (not just other anchors, since §2.4 only requires the signal on one side) for the best still-unused, opposite-signed, different-account transaction within `XFER_MATCH_AMOUNT_TOLERANCE_CENTS` ($1.00 default, covering a wire/ACH fee) and `XFER_MATCH_DATE_TOLERANCE_DAYS` (3 days default, matching §2.4's own ACH-settlement estimate), breaking ties by closest date, then closest amount, then lowest id. A candidate matched to one anchor can't also match another. This is an honest simplification, documented as such in the code: with several same-day, same-amount candidates on both sides, a different pairing could be equally valid — nothing in a transaction record can disambiguate that — so the guarantee is determinism (the same input always produces the same pairing) and per-pair correctness, not proof of the single best global pairing.

  **XFER-2's candidate pool** — `TransactionRepository.findUnmatchedTransferCandidates(userId)` — is every non-removed, settled (`pending: false`) transaction with no `transferGroupId` yet, for that user, full history, no date windowing. `pending: true` is excluded because `syncConnection.ts`'s `supersedePending()` (ING-9) carries `category` across to a pending transaction's posted replacement but not `transferGroupId`/`excludeFromCashFlow` — matching a pending row would risk an orphaned link the moment it settles under a new document id. Full-history, unwindowed scan is a deliberate Phase 1 simplification for this app's self-hosted, single-user scale, consistent with ADR-0025's "work list from the database, not a schedule/payload" principle elsewhere — revisit with real windowing/pagination if a multi-year transaction history ever makes this slow.

  **XFER-3** reuses `TransactionRepository.applyTransferMatch()`, already built and tested ahead of this story landing; the job's only addition is generating a `crypto.randomUUID()` per matched pair and calling it.

  **XFER-4's aging** happens in the same job run, over the same candidate pool, after matching: any transaction still unmatched, still transfer/payment-signal-tagged, whose category is currently `confirmed`, and whose transaction `date` (not `createdAt`) is older than `XFER_UNMATCHED_AGE_DAYS` (3 days default, §2.4's own example) gets `updateCategory()`'d to `{tier: 4, value: <unchanged>, status: "needs_review"}` — reusing the existing Tier 4 mechanism (`findNeedsReview()`) rather than inventing a parallel review surface. `value` is preserved rather than reset to `"Uncategorized"`: whatever Tier 1-3 already decided is still the best guess on record; this only asks a human to confirm it given a transfer signal that never found its other half. Already-`needs_review` transactions are skipped, making this idempotent across runs without extra bookkeeping. The manual "confirm as external" / "manually pair it" actions §2.4 describes have no UI to call them from yet, the same CAT-7/`apps/web` gap ADR-0027/ADR-0028 already flag — aging correctly routes into the queue `findNeedsReview()` already serves; acting on what's in it is CAT-7's problem, not XFER's.

  **XFER-5's rollup signal** follows `providerSync.ts`'s existing precedent exactly: `TransferMatchingResult` carries `touchedDayBuckets`/`touchedMonthBuckets` (same `utcDayStart`/`utcMonthStart` bucketing from `sync/normalize.ts`), returned on the job result and left deliberately unwired for `ANLY-2` to consume, rather than invented as a new mechanism. Only actual match writes record a bucket — aging changes `category.tier`/`.status`, neither of which any rollup aggregates over (`category.value` and `excludeFromCashFlow` are what `ANLY-4`/`ANLY-5` will read), so recording a bucket for an aging-only write would trigger a recompute nothing downstream needs.

- **ADR-0030 — WEB-0's chosen mockup direction ("Mockup A") is centralized as constants in `apps/web/src/design/tokens.ts`, not inlined per-component.** WEB-0 built two full mockup directions as a six-artboard `design`-skill canvas (login, dashboard overview, spending, each direction) rather than a single guess: "Mockup A", grounded in the project-local `taste-skill` framework's `minimalist-ui` sub-skill (warm monochrome, editorial minimalism) blended with `design-taste-frontend-v1`'s dashboard-hardening rules (single desaturated accent, monospace figures for dense data, no AI purple/blue); and "Mockup B", conventional shadcn/ui defaults (Inter, blue-600 primary) decided independently, with no taste-skill input, as the comparison baseline. `taste-skill`'s newer, larger `design-taste-frontend` (v2) was deliberately not used as Mockup A's basis — it states its own scope up front ("Landing pages, portfolios, and redesigns. Not dashboards, not data tables, not multi-step product UI.") and explicitly recommends other systems for dashboards/product UI instead, a direct mismatch with what this app is. The user picked Mockup A; palette and typography were otherwise undecided, so rather than let those choices leak into whichever component happens to need a color first, `apps/web/src/design/tokens.ts` is the one file WEB-1/WEB-5 will import into Tailwind's `theme.extend` (`FONT`, `RADIUS`, `UI_COLOR`, `STATUS_COLOR`, `TAG_PALETTE`) — changing the look later is a one-file edit, not a repo-wide find-and-replace. `CATEGORICAL_PALETTE`/the sequential hue are re-exported from the dataviz skill's already-validated default palette unchanged (the same list `DEFAULT_CATEGORY_SEEDS`, ADR-0028, cycles server-side) rather than re-derived, so a future accessibility re-validation has one place to run against. Deliberately excluded from this file: any category-name-to-color mapping. The mockups showed illustrative pairings ("Rent & Utilities" in categorical slot 1, etc.) purely for those screens' realism — real category colors are already a solved, per-user, editable DB field (`Category.color`, ADR-0028), so a static frontend mapping would either duplicate or fight that source of truth. The file has no framework import (no React) so it typechecks standalone ahead of WEB-1's Vite/Tailwind bootstrap.

- **ADR-0031 — Tailwind v4 for WEB-1's bootstrap, wired to `src/design/tokens.ts` via `@config`, not v4's CSS-native `@theme`.** WEB-1 needed a concrete Tailwind major version, and ADR-0030's language already assumed a JS-config `theme.extend` shape when it locked `tokens.ts` in as the single source of truth -- so the version choice had a real dependency, not just a "pick latest" default. Decision: Tailwind v4.3.3 (verified current stable against the npm registry, not assumed) via its official `@tailwindcss/vite` plugin -- no `postcss.config.js`/`autoprefixer`, which v3 needed and which the taste-skill's `design-taste-frontend-v1` sub-skill's "T4 CONFIG GUARD" specifically warns not to carry into a v4 project. v4's default path is a CSS-native `@theme` block, which would mean re-typing every token value into CSS -- exactly the duplication ADR-0030 exists to avoid. Instead, `src/index.css` uses v4's backward-compatible `@config "../tailwind.config.ts"` directive; `tailwind.config.ts` is a thin adapter importing `src/design/tokens.ts` into `theme.extend.{colors,fontFamily,borderRadius}` and holds no literal color/font/radius value of its own. React 19.2.8, Vite 8.2.2, and `@vitejs/plugin-react` 6.1.1 are likewise the current stable majors, cross-checked for mutual compatibility (`@vitejs/plugin-react@6`'s `peerDependencies` require `vite ^8.0.0`, which 8.2.2 satisfies) rather than picked from training-data memory of older majors. **The trap this hides:** `pnpm install` could not be run from this session's device-bridge shell for this story. `uname -a` there reports a Linux aarch64 VM bind-mounting the repo -- not the user's actual macOS machine -- confirmed by finding an already-installed `@esbuild/darwin-arm64` Mach-O binary (from a prior real `pnpm install` on the user's Mac) that the VM's Linux userspace cannot execute. Running `pnpm install` from inside that VM would resolve Linux-arm64 optional-dependency binaries (rollup, esbuild) into the very same `node_modules` the user's real Mac terminal reads for `pnpm dev`/`pnpm build` -- the identical failure class already hit once this project, surfaced when hand-tracing `matching.test.ts` needed a real vitest run and hit `Cannot find module '@rollup/rollup-linux-arm64-gnu'`. So for this story, `package.json`'s new dependencies were added by hand with versions confirmed via `npm view <pkg> version` (registry metadata only, safe from either platform) and `pnpm install` itself was deliberately left for the user to run on their own machine -- `tsc --noEmit` at handoff correctly reports every new import (`react`, `vite`, `@vitejs/plugin-react`, `@tailwindcss/vite`) as unresolved (TS2307) until that install happens, which is the expected state of an unreviewed bootstrap, not a defect in the added code. Whether Tailwind's `@config`-loaded `tailwind.config.ts` actually resolves its relative import of `src/design/tokens.ts` through Tailwind's own config loader is therefore unverified end-to-end; confirm by running `pnpm --filter @financial-os/web dev` and checking that `App.tsx`'s placeholder screen actually picks up the warm/terracotta palette rather than falling back to unstyled HTML.

- **ADR-0032 — React Router v7 for WEB-2's route table; React Query's cache (not an event bus or a raw redirect) is WEB-3/WEB-4's "shared 401 -> redirect-to-login" mechanism.** Two related choices this pair of stories needed. First, the router: `react-router-dom@7.18.3`, not the bare `react-router` package -- checking the npm registry (ADR-0031's habit, not assumption) found `react-router` itself has moved to a v8 major ahead of `react-router-dom`, which still depends on `react-router@7.18.3` internally; v8's standalone-SPA story wasn't verified, so the well-trodden, DOM-specific `react-router-dom` v7 (`BrowserRouter`/`createBrowserRouter`) was used instead of chasing an unverified newer major. `src/router.tsx`'s route tree nests two pathless layout routes (no `path` of their own): `ProtectedRoute` wraps `AppShell`, which wraps the five real pages. Kept as two layers rather than one merged component so a future authenticated-but-chromeless route (an onboarding flow, say) could sit directly under `ProtectedRoute` without inheriting the sidebar. Second, the 401 handling: rather than a custom event bus or a raw `window.location.href = "/login"` buried in the fetch layer, `apiFetch`'s `ApiError` is caught by a single `QueryCache` `onError` hook (`src/api/queryClient.ts`) that writes `null` into the `['auth','me']` query key every `useSessionQuery()` call (`src/auth/session.ts`) reads -- including `ProtectedRoute`'s. React Query's cache is already the source of truth for "am I logged in"; this reuses it instead of adding a second mechanism next to it, and makes the redirect naturally reactive (`ProtectedRoute` re-renders because its own query's data changed, not because something told it to navigate). **Known limitation, accepted:** every route in this app requires auth today, so "any 401, anywhere, means the session is gone" is a safe blanket rule. If a future endpoint is ever allowed to 401 for some other reason (a public-but-personalized route, say), it needs to opt out via its own query `meta` flag -- the handler comment in `queryClient.ts` flags this rather than silently becoming wrong later.

- **ADR-0033 — WEB-5's shadcn/ui components are hand-written, not scaffolded via the shadcn CLI; WEB-6 wires Testing Library into the jsdom setup SETUP-5 already had.** `npx shadcn init`/`add` needs to resolve the project it's scaffolding into (reading `package.json`, `tailwind.config.ts`, `tsconfig.json` paths) and fetches component source over the network -- doable in principle, but WEB-5 landed in the same pass as WEB-1, before `pnpm install` had ever been run (ADR-0031's gap), so there was no installed React/Vite for the CLI to detect and no guarantee the CLI's own dependency resolution would behave identically from this session's Linux VM. Decision: hand-write the same shape the CLI would have generated -- `src/lib/utils.ts`'s `cn()` (`clsx` + `tailwind-merge`, shadcn's own helper, byte-for-byte the standard implementation), and `src/components/ui/{button,card,badge,input}.tsx` using `class-variance-authority` for variants and `@radix-ui/react-slot` for `Button`'s `asChild` pattern -- styled against `tokens.ts`'s Tailwind classes (`bg-button-primary`, `rounded-sm`, etc.) rather than shadcn's own default palette/radii, same as the CLI output would need restyling to either way. `Badge` deliberately carries two separate variant families (`status-*` from the dataviz skill's reserved palette, `tag-*` from minimalist-ui's decorative pastels) rather than one merged enum, mirroring `tokens.ts`'s own STATUS_COLOR/TAG_PALETTE split and its doc comment about not mixing the two. For WEB-6: `vitest.config.ts`'s jsdom environment was already configured by SETUP-5, so this story only needed `@testing-library/react`/`@testing-library/user-event`, `@testing-library/jest-dom`'s `/vitest` entry point specifically (`src/test/setup.ts`, registered via `setupFiles`) rather than the classic bare import -- jest-dom's vitest-specific export exists so a project using vitest never pulls in a real `jest` dependency to get matchers like `toBeInTheDocument`. The convention was established against real code rather than a throwaway example: `client.test.ts` for `apiFetch` (plain vitest, mocked global `fetch`, real `Response` objects) and `ProtectedRoute.test.tsx` (RTL, `MemoryRouter` + a real `QueryClientProvider`) for the auth guard.

- **ADR-0034 — ANLY-1/ANLY-2's rollup job reconstructs historical net worth by walking each account's `currentBalance` backward through its transactions; asked and confirmed with the user before writing any of it, per AGENTS.md's "stop and ask" rule, since §4.4 never actually says how a *historical* `DailyBalanceSnapshot` gets computed.** Plaid exposes only each account's live balance -- there is no historical-balance API wired into this app -- so the only way to know what an account was worth on a past day is `balance(day) = currentBalance + sum(amount for that account's transactions dated after day)`, using the sign convention already established elsewhere (`matching.ts`, `PlaidProvider.ts`, `OllamaCategorizationProvider.ts`): a later debit (positive amount) means the balance was higher before it left; a later credit (negative amount) means it was lower before it arrived. This is the standard technique consumer finance apps use for exactly this Plaid limitation, and it's what makes ADR-0008's targeted recompute meaningful for `DailyBalanceSnapshot` specifically: an arbitrary past day (not just "today") can be corrected when a backdated `removed`/`modified` transaction touches its bucket. The alternative considered and rejected -- write only today's snapshot from `currentBalance` directly, never reconstruct the past -- would have quietly carved `DailyBalanceSnapshot` out of ADR-0008's "any rollup collection" recompute guarantee, even though §4.4 names it as one of the collections that mechanism covers.

  The pure math lives in `apps/worker/src/rollups/recompute.ts`'s `computeDailyBalanceSnapshot()` (day buckets) and `computeMonthlyRollup()` (month buckets, no reconstruction needed -- a month's income/expenses are just that month's transactions summed, correct for any month regardless of when it's computed). Supporting decisions bundled into this same story:
  - **Pending transactions are excluded from the balance reconstruction but included in cash flow.** `currentBalance` is documented as an account's settled balance, so mixing an uncertain pending-inclusion assumption into a dollar figure risks a wrong number; excluding it just means a pending purchase's effect on net worth lags a beat until it posts. Cash flow has no such risk -- `syncConnection.ts`'s `supersedePending()` already guarantees a transaction is never simultaneously live-pending and live-posted (the pending row flips to `isRemoved` the moment its posted counterpart lands) -- so including pending transactions there is safe and correct, and means "money spent this month" reflects real activity as it happens rather than only once it settles.
  - **Asset/liability classification needs no sign flip.** `depository`/`investment` accounts sum into `assets`; `credit`/`loan` accounts sum into `liabilities` directly, since Plaid's own `currentBalance` convention for those account types is already a positive amount owed.
  - **An account not yet linked as of a given day is omitted entirely** from that day's per-account list and from `assets`/`liabilities`/`netWorth` -- using `Account.createdAt` (truncated to UTC midnight, inclusive) as the "linked" boundary, since `AccountRepository.upsertFromSync()` always runs before any of that account's transactions are recorded. This is the gap ANLY-11's chart renders, matching §4.2's "the series starts at that account's first snapshot date rather than implying a $0 balance."
  - **A real bug, caught before anything used it: `MonthlyRollup.ts`'s original field comments had `income`/`expenses` inverted** relative to the sign convention documented everywhere else in this codebase -- "income: sum of positive amounts" when positive actually means money *leaving* the account. No rollup job existed yet to have computed a wrong number from it (this story is what first writes to that collection), so this was a stale doc comment, not a live-data bug -- fixed alongside building the real computation, comments now read `income` = magnitude of negative (arriving) amounts, `expenses` = sum of positive (leaving) amounts.
  - **Persistence detail:** `RollupRepository.upsertDailyBalanceSnapshot()`'s `accounts` input takes plain `accountId: string`, not `mongoose.Types.ObjectId` -- `computeDailyBalanceSnapshot()` is pure logic with no business depending on mongoose, and neither does `apps/worker` generally (only `packages/db` lists mongoose as a direct dependency, ADR-0005's layering); the `ObjectId` construction happens inside `RollupRepository`, the one place in this call chain allowed to know mongoose exists.
  - **The rollups queue has no per-user dedup `jobId`, unlike provider-sync/categorize-llm/transfer-matching's.** Those three derive their whole work list from the database at run time, so collapsing a duplicate enqueue onto an existing job is safe. A rollups job instead carries a *specific* bucket list as its payload (`packages/shared/src/queues.ts`'s `RollupJobData`); deduping by user would silently drop whichever enqueue lost the race, along with the buckets it carried, and nothing would ever recompute them. Recompute is idempotent (each bucket is derived fresh from current `Account`/`Transaction` state, never an incremental delta), so redundant overlapping jobs are harmless -- there's no correctness reason to dedup, only a negligible cost at single-user scale.
  - **`apps/worker/src/queues/providerSync.ts` and `transferMatching.ts` both call `triggerRollups()` independently**, each passing its own `touchedDayBuckets`/`touchedMonthBuckets`, rather than chaining a single trigger off transfer-matching (the last pipeline stage). Transfer-matching's own touched buckets only cover buckets its matching pass touched -- a small subset of what provider-sync ingests -- so a trigger fired only from the last stage would miss most ordinary, non-transfer transactions.
- **ADR-0035 — ANLY-3/ANLY-4/ANLY-5/ANLY-6's dashboard read endpoints: required `start`/`end` query params (no default window), day/month gap-filling done server-side, and `$facet` used only where the collection it queries is actually large.** Four bundled decisions, each a genuine gap ARCHITECTURE.md left open:
  - **No default date range.** §4.2 never specifies what "no range given" should mean, and guessing one (last 30 days? last 12 months?) is a product decision, not an engineering one -- so `/api/analytics/{net-worth,cash-flow,spending-categories}` all 400 rather than silently picking a window. The frontend (ANLY-9) is what decides a sensible default and always sends explicit `start`/`end`.
  - **Net worth gap-filling is carry-forward, not zero-fill** (`apps/api/src/analytics/netWorth.ts`'s `fillNetWorthSeries()`): the rollup job (ADR-0034) only writes a `DailyBalanceSnapshot` for a day that actually had transaction activity, so a quiet day in the middle of a range needs its value carried forward from the most recent real snapshot -- including one from *before* the requested range, which is why the route also calls a new `RollupRepository.getLatestNetWorthSnapshotBefore()` and merges it in before filling. A day before this user's first-ever snapshot stays a real gap (§4.2's existing language). The endpoint's response also grew a 5th field beyond §4.2's originally-sketched `{date, netWorth, assets, liabilities}`: a per-day `accounts: [{accountId, balance}]` array, carried straight through from `DailyBalanceSnapshot`'s own stored shape -- ANLY-11's per-account gap rendering has no other source for "which accounts existed on this day," and this was the only way to deliver it without inventing a second endpoint.
  - **Cash flow gap-filling is real zero-fill, not carry-forward** (`apps/api/src/analytics/cashFlow.ts`'s `fillMonthlyCashFlowSeries()`) -- deliberately different logic from net worth's, because the underlying semantics differ: a month with no `MonthlyRollup` row genuinely had `$0` income and `$0` expenses (ADR-0008 only writes a bucket for a month that had transactions), whereas a day with no `DailyBalanceSnapshot` does NOT mean a `$0` balance -- it means "nothing changed," which is a fact about the *delta*, not the *value*. Net worth is a point-in-time quantity; cash flow is a sum over the period, and a sum with nothing to add is legitimately zero.
  - **`getCategoryDistribution()`'s `$group` filters to `amount > 0` (spending only, Plaid convention) and excludes `excludeFromCashFlow: true`** (`packages/db/src/repositories/TransactionRepository.ts`) -- an income deposit or an internal transfer between the user's own accounts isn't "spending" in any category sense, matching the same exclusions `findForCashFlow()` already applies.
  - **`$facet` (ANLY-6) is used for the spending-categories comparison but NOT for cash-flow's** -- `getCategoryDistributionComparison()` runs both `range` and `compareRange` through one `$facet` aggregation against the raw, unbounded-growth `Transaction` collection (exactly §4.2's own worked example), pre-filtering to the union of both ranges so Mongo scans the combined window once. Cash flow's comparison instead just calls `RollupRepository.getMonthlyRollup()` twice, against the small, already-indexed, write-time-aggregated `MonthlyRollup` collection -- two cheap point queries cost about the same as one `$facet` there, so the extra aggregation complexity buys nothing. Same "write-time aggregation for anything cheap to precompute, on-demand `$facet`/`$group` only where it's actually needed" principle §3.2 already states, applied consistently rather than mechanically forcing `$facet` everywhere ANLY-6's ticket text mentions comparison.
  - **Response shape for cash-flow/spending-categories is always `{current, compare}`**, with `compare: null` when no `compareStart`/`compareEnd` was given -- one consistent contract for the route regardless of whether a comparison was requested, rather than the shape morphing between a bare array and an object depending on query params. Net worth stays a plain array; a "compare" concept doesn't map naturally onto a continuous point-in-time series the way it does onto two summed periods.
- **ADR-0036 — ANLY-7's subscription-detection heuristic, implemented exactly to §4.2's Phase 1 spec (monthly/annual only, no invented third bucket), as a nightly cron-scheduled BullMQ job over every user.** `apps/worker/src/subscriptions/detect.ts`'s `detectSubscriptions()` groups a user's positive-amount, non-removed, non-pending, non-transfer-matched transactions by `merchantNameNormalized`, requires at least 3 (§4.2), checks every amount falls within ±15% of the group's median (covers a subscription price change without accepting an unrelated one-off purchase at the same merchant), then computes the population standard deviation of the gaps between consecutive transactions and rejects anything above 5 days -- §4.2's "simple standard-deviation-on-interval check" -- before checking whether the *average* interval clusters within ±5 days of 30 or ±20 days of 365. A merchant matching neither window (a weekly charge, an irregular one) is not detected at all, even though `Subscription.frequency`'s type allows a third `"other"` value -- §4.2 names only the two clusters for Phase 1, and inventing a catch-all the spec never describes would be guessing at criteria nobody asked for; `"other"` stays reserved for a future heuristic (e.g. weekly/quarterly detection) this function doesn't implement.

  Queue shape: unlike provider-sync/categorize-llm/transfer-matching/rollups, there's no per-event trigger for "check if this user's spending pattern looks recurring" -- it's explicitly a nightly batch job (§4.2's own words), so `apps/worker/src/queues/subscriptions.ts` follows `providerSyncScheduler.ts`'s pattern instead: a repeating BullMQ job (`upsertJobScheduler`, idempotent by a stable scheduler id) wakes on a cron pattern (`SUBSCRIPTION_DETECTION_CRON`, default `0 3 * * *` — 3am UTC, a quiet hour) and does its own database-driven fan-out, calling the new `UserRepository.findAllIds()` to enumerate every user rather than assuming the single hardcoded user this app is built for — the same choice `providerSyncScheduler.ts`'s `findSyncable()` already makes for connections. Each run also reconciles lapses: a merchant active from a previous run that this run's detection no longer confirms gets `SubscriptionRepository.markInactive()` (`Subscription.active: false` — a lapsed subscription, per that field's own doc comment, never a deleted record) rather than being left stale forever.

  Two small supporting decisions: `TransactionRepository.findSubscriptionCandidates()` is a full-history scan with no date windowing, the same accepted Phase 1 simplification `findUnmatchedTransferCandidates()` already documents for this app's self-hosted, single-user scale; and the new `SubscriptionRepository.upsertDetected()` takes plain-string `transactionIds`, not `mongoose.Types.ObjectId[]`, constructing the real ObjectIds internally — the same mongoose-stays-inside-packages/db boundary ADR-0034 established for `RollupRepository.upsertDailyBalanceSnapshot()`.
- **ADR-0037 — ANLY-8's SSE route listens to three queues (provider-sync, transfer-matching, rollups), resolves each completed job's owning user via a `Job.fromId()` lookup (BullMQ's `QueueEvents` payload doesn't carry job data), and filters every event to the connected client's own session before writing it.** §4.3 sketches `sync.completed`/`transfer.matched` as examples; a third, `rollup.completed` (fired off the queue ANLY-1/ANLY-2 built), was added because it's the most direct "the dashboard's actual numbers changed" signal in this pipeline -- provider-sync and transfer-matching are upstream stages a chart doesn't read from directly, while a rollup finishing is exactly when `DailyBalanceSnapshot`/`MonthlyRollup` (what the ANLY-3/ANLY-4 endpoints actually serve) become stale-and-then-fresh. `events/publisher.ts` keeps this decoupled from Fastify entirely -- a plain `Set<Listener>` pub/sub the SSE route (`routes/events.ts`) is the only subscriber of today, but isn't required to be.

  **Why a job lookup is needed at all:** `QueueEvents`'s own `completed` event payload is just `{jobId, returnvalue}` -- no access to the job's original `data`, which is what actually identifies *whose* dashboard an event belongs to (provider-sync's job data has `connectionId`, requiring one more hop through `ConnectionRepository.findById()` to reach `userId`; transfer-matching's and rollups' job data already carry `userId` directly). `Job.fromId(queue, jobId)` retrieves it, using one lightweight lookup `Queue` instance per listened queue rather than resurrecting the private producer queues already declared in `apps/api/src/queues.ts`.

  **Connection hygiene:** `QueueEvents` consumes via Redis Streams with a blocking read under the hood -- the same reason `apps/worker`'s `createRedisConnection()` gives every `Worker` its own connection applies here, so `apps/api/src/redis.ts` grew a third connection factory, `getEventsConnection()`, separate from the session client and the BullMQ producer connection.

  **The route itself** (`GET /events`, requireAuth-gated, registered at `/events` rather than under `/api` -- matching the dev-server proxy WEB-1/WEB-3 already wired up, and the containerized Caddyfile's routing, both of which already carve `/events` out as its own path) uses Fastify's `reply.hijack()` to take manual control of the raw response and stream `event: <type>\ndata: <json>\n\n` lines. A leading `:connected\n\n` comment line gives the browser's `EventSource` something to fire `open` on immediately rather than waiting for this user's first real event, and a `:heartbeat\n\n` comment every 30s keeps an idle connection from being silently timed out by a proxy sitting between the browser and this server -- both are SSE comment lines (`:`-prefixed), which the `EventSource` spec requires clients to ignore, so neither ever reaches ANLY-10's message handler.

- **ADR-0038 — ANLY-9/ANLY-10/ANLY-11's frontend build: hand-rolled SVG charts instead of Recharts/Chart.js, reusing WEB-2..6's already-established flat `pages/`+`api/` structure instead of §4.1's sketched `/routes`+`/features` tree, and a coarse invalidate-everything SSE client.** Three deviations from what ARCHITECTURE.md/BACKLOG.md literally say, each with a concrete reason, not an oversight:
  - **No charting library.** §4.2's heading names "Recharts/Chart.js" and ANLY-9's own ticket text repeats it, but neither `apps/web/package.json` nor any WEB-epic story ever added one, and the section's actual content is entirely about the *data contract* (chart-ready aggregates, not raw transactions) rather than a frontend implementation requirement. Adding either library now would mean a new `package.json` dependency this session cannot `pnpm install` for itself (ADR-0031's established constraint), for a purely cosmetic gain: `components/charts/{NetWorthChart,CashFlowChart,SpendingCategoriesChart}.tsx` are plain SVG built against the `dataviz` skill's method (validated `CATEGORICAL_PALETTE`/`SEQUENTIAL_HUE`/`STATUS_COLOR` from `design/tokens.ts`, ADR-0030 — re-validated once already, not re-run here — thin 2px lines, a hover crosshair + tooltip on the line/bar charts, a `<details>`-disclosed data table for accessibility, categorical hues in fixed rank order never cycled). This is the same category of call as WEB-5 hand-writing shadcn's component shapes instead of running its CLI (ADR-0033) — match the method, skip the dependency, for the same "can't install it from here anyway" reason.
  - **Flat `pages/`+`api/`+`components/` structure, not `/routes`+`/features`.** §4.1 sketches a `/routes` + `/features/<name>/{Chart,useQuery}` split; WEB-2/WEB-3 already built (and BACKLOG.md already checked off) a flatter `pages/`, `api/`, `auth/`, `layout/`, `components/ui/` tree with no such split, before this epic ever started. Introducing a second, parallel structure for just the four new pages would fragment the app for no reader's benefit — `api/analytics.ts` (typed fetch + React Query hooks together, one file per domain, matching `auth/session.ts`'s existing pattern) and `pages/{NetWorth,CashFlow,Spending,Subscriptions}Page.tsx` (matching `pages/LoginPage.tsx`) are the continuation of the convention actually on disk, not a new one.
  - **`review-queue` is not a 5th ANLY-9 folder**, despite ANLY-9's own ticket text listing it alongside the four analytics pages. WEB-2's own delivery note (BACKLOG.md line 96) already separates "CAT-7 (Tier 4 review queue UI)" from "ANLY-9..11 (the actual dashboard pages)" when describing why the WEB epic exists, and CAT-7 is a distinct, still-open ticket in the CAT epic with real scope of its own (list `needs_review` transactions, manual category correction, triggers the CAT-6 write-back loop) — none of which is an analytics chart. `/review` keeps rendering `PageStub` until CAT-7 lands; building an empty or stubbed-out review folder here would be scope invented ahead of the ticket that owns it.
  - **`SpendingCategoriesChart` assigns `CATEGORICAL_PALETTE` by rank, not by reading `Category.color`.** `TransactionRepository.getCategoryDistribution()`/`getCategoryDistributionComparison()` (ADR-0035) group by `category.value` only and return `{category, total}` — no color, because the aggregation never joins the `Category` collection. `design/tokens.ts`'s own doc comment ("apps/web reads `Category.color` from the API and never assigns one client-side") describes the transaction-row-badge use case elsewhere in this app, not this chart, which has no per-category color in its response to read. Assigning the validated palette by sorted rank (top 8, already `$sort`-ed desc by the aggregation) and folding anything past slot 8 into a neutral-gray "Other" bucket is exactly the dataviz skill's own non-negotiable for this situation ("a 9th series is never a generated hue — it folds into Other").
  - **`lib/useDashboardEvents.ts` invalidates the entire `["analytics"]` React Query prefix on any of the three SSE event types, not a per-event-to-per-query mapping.** All four analytics queries (`api/analytics.ts`) are cheap indexed reads (ADR-0035) — a redundant refetch triggered by, say, `sync.completed` when only cash flow actually changed costs nothing worth engineering around, and a coarse rule can't drift out of sync with itself as new analytics queries are added (a bespoke per-event map could silently miss a new query).
  - **ANLY-11's gap flag is per-day coverage, not a per-account series.** `components/charts/netWorthGaps.ts`'s `flagPartialCoverage()`/`toSegments()` (pure, tested independent of the chart component, `netWorthGaps.test.ts`) compares each day's `accounts.length` (the field ADR-0035 added to the net-worth response) against the series-wide maximum and splits the line into solid/dashed runs at the coverage boundary — a full per-account multi-line breakdown was considered and rejected as more chart surface than a Phase 1 dashboard needs; the dashed segment already answers "was this number complete," which is what a partial-history gap is really warning about.
  - **ANLY-1/ANLY-2's rollup writes are a targeted Mongoose upsert per bucket (`RollupRepository.upsertDailyBalanceSnapshot()`/`upsertMonthlyRollup()`), not a `$merge`-terminated aggregation pipeline**, even though both ANLY-1's ticket text and §4.4 name `$merge` specifically. The actual per-day balance reconstruction (ADR-0034's backward-walk) needed to be a plain, hand-traceable, unit-testable function — `apps/worker/src/rollups/recompute.ts`'s `computeDailyBalanceSnapshot()`/`computeMonthlyRollup()` — precisely because AGENTS.md's TDD convention names "rollup bucket-tracking" as one of its own worked examples of pure logic requiring a failing test written first; that convention is very hard to honor for logic living entirely inside an opaque `$merge` pipeline with no separable function `recompute.test.ts` could call directly. The practical effect is identical to what §4.4 asks for — only the touched buckets are recomputed, and the write is an idempotent upsert that overwrites exactly the affected document — just reached via `TransactionRepository`/`AccountRepository` reads + an ODM upsert instead of an all-in-Mongo pipeline. Flagged here rather than silently diverging: **ANLY-12's tests (`RollupRepository.test.ts`, `TransactionRepository.test.ts`, `SubscriptionRepository.test.ts`, all real `mongodb-memory-server` integration tests) cover `$facet` and `$group`/`$sort`/`$project` aggregations, but not `$dateTrunc` or `$merge` specifically, since this codebase does not use either stage anywhere** (confirmed by search, not assumption — the only hits are code comments describing the mechanism this ADR explains was implemented differently).
- **ADR-0039 — WEB-7's Connect Account page: a persistent `/accounts` route (not an empty-state-only prompt), a new provider-neutral `GET /api/accounts` read endpoint backed by a pure Account+Connection join, a small dedicated `GET /api/config` for the Sandbox badge, and `react-plaid-link@5.0.0` as the first UI dependency this project has ever added.** Four bundled decisions:
  - **Persistent nav item, not an onboarding-only empty state.** The two options from BACKLOG.md's own open question, decided here rather than left open: `ConnectionRepository.findByUserId()`/`AccountRepository.findByUserId()` already return arrays (AUTH-3/ADR-0018 never assumed exactly one Connection), so an empty-state-only prompt would work for linking the *first* account and then have nowhere to send a user who wants to link a second one later. `/accounts` (`apps/web/src/pages/AccountsPage.tsx`) is a real page in `AppShell`'s nav -- placed first, ahead of the four analytics destinations, since connecting a bank is the one prerequisite all of them share and none of them have anything to show without it.
  - **`GET /api/accounts`** (`apps/api/src/routes/accounts.ts`) is new, provider-neutral (ADR-0004), and deliberately separate from `routes/plaid.ts` (which stays scoped to the Link flow itself -- token creation + exchange) and from `routes/analytics.ts` (aggregates, not raw linked-account state). Its join logic -- an `Account` carries no status of its own, that lives on its parent `Connection` -- is a pure, test-first function, `accounts/list.ts`'s `buildAccountList()` (`list.test.ts`, 7 tests hand-traced), matching this codebase's existing convention of keeping anything with real branching logic separable from its route handler. A `connectionId` with no matching `Connection` (shouldn't happen given `routes/plaid.ts`'s write path, but the read side queries the two collections independently and has no way to guarantee it) defaults to `"error"` status rather than throwing.
  - **`GET /api/config`** (`apps/api/src/routes/config.ts`) exists solely to answer "is this deployment pointed at Plaid Sandbox," for the Sandbox badge `AppShell.tsx` now renders next to the wordmark. Deliberately not folded into `/api/auth/me` (identity, not deployment config) or `/api/plaid/link-token` (only fetched on demand, after the badge already needs to be showing) -- its own tiny route, one field today, `staleTime: Infinity` on the frontend query since a deployment's Plaid environment can't change without a redeploy. Deployment-wide by explicit choice, not per-`Connection` -- `PLAID_ENV` is a single env var this whole `apps/api` process runs under, and tracking it per-account was considered and set aside as unneeded complexity for a solo self-hosted deployment that only ever runs one environment at a time.
  - **`react-plaid-link@5.0.0`**, added to `apps/web/package.json` by hand (registry-checked against `registry.npmjs.org`, not assumed) -- its peer range, `"react": "^16.8.0 || ^17 || ^18 || ^19"`, was cross-checked against this app's actual `react@^19.2.8` before picking it, the same discipline ADR-0031 established for WEB-1's original dependency set. Same caveat as every dependency added since this session started (ADR-0031's "trap"): this session cannot run `pnpm install`, so `AccountsPage.tsx`'s `tsc --noEmit` currently reports the import itself as unresolved, plus one cascading `noImplicitAny` on `usePlaidLink`'s `onSuccess` callback parameter that contextual typing will resolve on its own the moment the real package installs -- not a second, independent bug.
  - **`useAccountsQuery()`'s query key (`["accounts"]`) stays outside the `["analytics"]` prefix** ANLY-10's SSE client invalidates wholesale. Linking an account doesn't itself change any dashboard number -- the sync job that follows does, and that already has its own `rollup.completed` event. `useExchangePublicTokenMutation()` instead invalidates `["accounts"]` directly on success, so the newly-linked account appears in the list immediately rather than waiting on a sync cycle.
- **ADR-0040 — WEB-8's Transactions page: offset pagination via a `pageSize + 1` fetch-and-slice (no `countDocuments()`), a `status` filter built into the same route/repository method from day one for CAT-7 to reuse, and a `TransactionsTable` component extracted from the page specifically so CAT-7 can import it rather than re-copy it.** Three bundled decisions:
  - **Pagination is offset-based (`page`/`pageSize`), not a cursor**, matching this app's established "the simplest thing that works at single-user scale" posture (`findUnmatchedTransferCandidates()`, `findSubscriptionCandidates()`'s accepted full-history-scan simplifications). `TransactionRepository.findPageForUser()` fetches `pageSize + 1` rows and slices off the extra one to compute `hasMore`, rather than running a second `countDocuments()` query -- there's no "page 1 of N" requirement anywhere in BACKLOG.md's WEB-8 text, just "is there a next page," which the extra row already answers at half the query cost.
  - **`findPageForUser()`'s `status` option and `GET /api/transactions?status=`** exist now, not deferred to CAT-7, because BACKLOG.md's own CAT-7 rescoping note already named this exact mechanism ("`GET /api/transactions?status=needs_review` or similar") as what the review queue would need. Building it as part of the same method/route WEB-8 needed anyway avoids CAT-7 needing a second, near-duplicate query later -- `category.status` is filtered only when `status` is passed, so the plain ledger view (WEB-8) and the review queue (CAT-7) are the same code path with one optional parameter distinguishing them.
  - **The join logic is a pure, test-first function** (`apps/api/src/transactions/list.ts`'s `buildTransactionList()`, `list.test.ts`, 7 hand-traced tests), the same convention `accounts/list.ts` established for WEB-7: a `Transaction` stores only `accountId` (`packages/db/src/models/Transaction.ts`), so display needs a join against `Account` for institution/subtype, and an orphaned `accountId` (no matching `Account`) falls back to a placeholder rather than throwing, mirroring `buildAccountList()`'s orphaned-`connectionId` handling. Unlike `buildAccountList()`, this does NOT re-sort its input -- the transactions it receives already arrived sorted and paginated by the repository query, and re-sorting a single page of an already-ordered result would be pointless at best.
  - **`apps/web/src/components/TransactionsTable.tsx` is its own file, not inlined in `TransactionsPage.tsx`.** BACKLOG.md's CAT-7 rescoping note says explicitly: "this should reuse WEB-8's transaction-row component and list page, filtered to `needs_review`." Extracting the table now, with an optional `renderRowAction` render-prop slot already in its signature (unused by WEB-8's plain ledger view, but reserved for CAT-7's category-correction control), means CAT-7 can import a real component instead of copy-pasting a table and drifting from it. Whether CAT-7 ends up reusing this component as-is or needs its own variant is still an open question to resolve when that story is actually built -- this is a reasonable bet given the ticket text, not a guarantee.
  - **`useTransactionsQuery()`'s query key (`["transactions", page, pageSize, status]`) stays outside both the `["analytics"]` prefix ANLY-10's SSE client invalidates and `["accounts"]`.** A transaction list going stale isn't a "dashboard number changed" event in the same sense net worth/cash flow are, and it isn't accounts-linking either -- it has its own staleness triggers (a sync completing, a category correction), which CAT-7's mutation is expected to invalidate directly (`queryClient.invalidateQueries({queryKey: ["transactions"]})`), the same pattern `useExchangePublicTokenMutation()` already established for `["accounts"]`. `placeholderData: keepPreviousData` (React Query v5's rename of v4's `keepPreviousData: true`) keeps the current page's rows visible while the next page loads, rather than flashing to a loading state on every click.
- **ADR-0041 — CAT-7's Tier 4 review queue: `/review` reuses WEB-8's `TransactionsTable`/`useTransactionsQuery` wholesale (only `status: "needs_review"` differs), the manual-correction route writes its own CAT-6 write-back directly against `MerchantRuleRepository` rather than importing `apps/worker`'s `writeBack.ts`, and a new ownership-scoped repository method replaces the worker-only `updateCategory()` for anything reachable from an HTTP request.** Four bundled decisions:
  - **`pages/ReviewPage.tsx` is almost entirely `pages/TransactionsPage.tsx`** with one query parameter changed (`status: "needs_review"`) and `TransactionsTable`'s `renderRowAction` slot filled in -- exactly what BACKLOG.md's CAT-7 rescoping asked for ("reuse WEB-8's transaction-row component and list page"), not a guess at what that might mean. A transaction leaving `needs_review` (this page's own correction control does that) simply stops matching the query's filter on the next refetch and disappears from the list -- no special-case removal logic needed, the existing pagination/empty-state code already handles "the list got shorter."
  - **`PATCH /api/transactions/:id/category`'s CAT-6 write-back is built inline against `MerchantRuleRepository.upsertExact()`, not by importing `apps/worker/src/categorize/writeBack.ts`'s `buildMerchantRuleWriteBack()`.** `apps/api/package.json` has never depended on `apps/worker` (confirmed, not assumed -- it depends on `@financial-os/db`/`providers`/`shared`/`config` only), and `writeBack.ts`'s own doc comment explicitly anticipated this exact fork: "CAT-7's manual-correction path is expected to call this (or skip straight to `MerchantRuleRepository`) with `"manual"` once it exists." The function's only real logic -- skip the write-back unless `category.status === "confirmed"` -- is moot here anyway, since a manual correction is by definition a confirmed decision (there's no "needs_review" outcome for a human choosing a category the way there is for Tier 3's confidence threshold), so nothing is lost by not calling it.
  - **`TransactionRepository.updateCategoryForUser(userId, transactionId, category)` is a new method, not a `userId` parameter bolted onto the existing `updateCategory()`.** Every current caller of `updateCategory()` (`categorizeLlm.ts`, `syncConnection.ts`, `transferMatching.ts`) runs inside a worker job with no authenticated user to scope against, and already trusts the `transactionId` it was handed (ADR-0025's "work list from the database" jobs). The new method scopes its Mongo filter to `{_id: transactionId, userId}` together -- the same ownership check `CategoryRepository.updateColor()` already established for CAT-10 -- so one user can never correct another's transaction via a guessed id. It also catches mongoose's `CastError` and returns `null` rather than letting it escape: this route is the first place in `apps/api` that feeds a raw client-supplied string into a Mongo `_id` filter, and an invalid id should 404 the same as a genuinely-missing one, not 500 with a stack trace.
  - **`GET /api/categories` is read-only; there is no create/rename/recolor route yet.** `CategoryRepository.create()`'s own doc comment flagged CAT-7 as where a create route was expected to land, but BACKLOG.md's actual CAT-7 scope is "list `needs_review` transactions, manual category correction, triggers write-back" -- not a category-management UI. `ReviewCategoryControl.tsx` instead falls back to a free-text input when a user's `Category` collection is empty (Tier 3 hasn't run long enough to seed it yet, `CategoryRepository.seedDefaults()`'s only caller) or when `GET /api/categories` itself errors -- both cases render the same fallback, an honest simplification rather than a distinct error state, so a correction is never blocked on category-list plumbing. Turning that free-text value into a real, colored `Category` row is left for whenever CAT-9's actual "manage categories" UI is scoped.
- **ADR-0042 — Two gaps found by a pre-validation self-audit, both fixed in the same pass: no browser path to create the bootstrap account (AUTH-6), and Plaid Link exchange never actually triggered a transaction sync (ING-13, previously untracked anywhere in BACKLOG.md).** Neither surfaced from ticket checkboxes -- AUTH-6 was already logged but easy to read as low-severity from its title, and the sync-trigger gap wasn't tracked as a ticket at all until this audit found it by reading `routes/plaid.ts` and `queues.ts` directly. Both are documented together since they were found together and both block the same thing: a first-time user's very first few minutes with the app.
  - **AUTH-6 -- `/register` is always reachable, not conditionally shown only when bootstrap is actually open.** `POST /api/auth/register` (AUTH-3/ADR-0018) already correctly gates itself -- open pre-bootstrap or for an already-authenticated user adding another, `403` otherwise -- so there was never a missing rule, only a missing page to reach it from. Building a second, unauthenticated "is registration open" endpoint just to decide whether to show a link would be new surface area for a cosmetic improvement; the backend's existing `403` ("registration is closed") is already a clear, self-explanatory answer, the same shape most self-hosted single-tenant software gives a used-up setup link. `LoginPage.tsx` gained a permanent "Create an account" link on the same logic.
  - **ING-13's root cause: `PlaidProvider.createConnection()` (`packages/providers/src/plaid/PlaidProvider.ts`) only ever calls Plaid's `accountsGet` -- never `transactionsSync`.** Transaction data has only ever entered this app through the `provider-sync` queue (ING-4), and nothing called `requestProviderSync()` from the exchange path -- its only caller was the webhook handler (`routes/webhooks.ts`). ING-3's own ticket text ("30-day backfill via `days_requested`") describes what `createLinkToken()` asks Plaid's consent screen to authorize, not that the backfill happens automatically once Link finishes -- a real gap between the ticket's wording and its actual behavior, clarified with an addendum on ING-3 itself rather than rewritten. The fix is a single new call in `routes/plaid.ts`'s exchange handler: `requestProviderSync(connection._id.toString())`, immediately after the Connection/Accounts are persisted. `queues.ts`'s own doc comment on `requestProviderSync()` had already named this exact case ("a webhook, ING-8's scheduled poll, or **a manual re-sync from the UI**") as a call site the per-connection dedup key was designed to cover -- this wires up the call site that comment was written for, not a new mechanism. Wrapped in a try/catch that logs rather than fails the request: the Connection/Accounts are already safely persisted by that point, and a Redis hiccup on the enqueue shouldn't turn a successful bank link into an error response.
  - **A second, related fix bundled in: `GET /api/accounts` now surfaces each account's `connectionLastSyncedAt` (ISO string or `null`), and a new `POST /api/accounts/:connectionId/sync` lets a user force a re-sync on demand.** Fixing only the initial-sync trigger would still leave every *subsequent* sync invisible -- `PROVIDER_SYNC_POLL_INTERVAL_MS` is 5 minutes in this deployment's `.env` but the app has no way to show that, and `PLAID_WEBHOOK_URL` is unset locally (Plaid can't reach a laptop), so the scheduled poll is this deployment's only ongoing trigger with zero user-facing feedback. The manual-sync route is the same `requestProviderSync()` entry point, ownership-checked by reading the `Connection` and comparing `userId` (a read-then-compare, not a scoped repository method like ADR-0041's `updateCategoryForUser()` -- there's no write here to fold the check into). `ConnectionRepository.findById()` picked up the same defensive `CastError`-to-`null` handling `TransactionRepository.updateCategoryForUser()` established in ADR-0041, since this route is the first caller to feed it a raw client-supplied id. `useAccountsQuery()` gained a 30-second background poll so a completed sync's new timestamp actually shows up without a manual page reload -- shorter than `api/analytics.ts`'s existing 5-minute dashboard-staleness poll, since "did my sync finish" is a tighter feedback loop than "did a chart change."
- **ADR-0043 — ACCT-1's account nickname is a field a provider resync can never touch: `Account.nickname` stays out of `UpsertAccountInput` entirely, rather than being written and then re-guarded against.** `AccountRepository.upsertFromSync()`'s `$set: input` overwrites every field `UpsertAccountInput` types, unconditionally, on every sync (`institutionName`/`officialName`/balances -- all provider-owned, refreshed on purpose each time Plaid reports new values). A user-chosen nickname is the opposite: it should never change unless the user changes it. Two ways to get that: (a) add `nickname` to `UpsertAccountInput` and have every call site that builds a sync payload carefully omit or preserve it, or (b) never type it into the sync path's input shape at all, so there's nothing for a future edit to `routes/plaid.ts`/`providerSync.ts` to accidentally start passing through. (b) was chosen -- the same reasoning `TransactionRepository.upsertFromSync()` already established for `category` via `$setOnInsert` (that field's own doc comment: "it is the one field on this document the *app* owns rather than the provider"), except stricter: `category` still gets set once, on insert, by the sync path itself, where `nickname` has no sync-time value at all and must only ever come from `AccountRepository.updateNickname()` (`{_id: accountId, userId}`-scoped, the same ownership check `CategoryRepository.update()`/`TransactionRepository.updateCategoryForUser()` use elsewhere). A TypeScript compile error is the enforcement mechanism: any future code trying to pass `nickname` into `upsertFromSync()`'s `input` fails to typecheck, rather than a runtime `$set` silently clobbering a name the user picked. `nickname: null` (not an empty string) is how `updateNickname()` clears it, so the display fallback (`nickname ?? officialName ?? institutionName`, `AccountsPage.tsx`) falls through correctly instead of rendering a blank name -- the same empty-clears-via-`$unset` convention `TransactionRepository.updateMerchantNameOverrideForUser()` (CAT-13) uses for the same reason.
- **ADR-0044 — CAT-15's `CategorySelect.tsx` wraps `@radix-ui/react-select`, the first Radix primitive this app depends on beyond `@radix-ui/react-slot` (WEB-5's `Button` `asChild`), superseding ADR-0033's "no Radix Select in this app's component set yet" precedent now that rendering `{icon} {name}` per option is a real requirement, not a nice-to-have.** A native `<select>`/`<option>` can only ever render plain text in every real browser -- no icon, no swatch -- so once `ReviewCategoryControl.tsx` (CAT-7) and `TransactionsPage.tsx`'s WEB-10 category filter both needed to show a category's icon per row, styling the existing native select stopped being an option; the choice was between hand-rolling a fully keyboard-accessible, focus-managed listbox (real surface area: arrow-key navigation, typeahead, focus trapping, portal positioning, screen-reader roles) or adding a dependency for it. Decided: add the dependency. `@radix-ui/react-select@2.3.7` (registry-checked against `registry.npmjs.org` -- ADR-0031's habit, not assumed; `2.3.8`/`3.0.0` exist only as pre-release `-rc` tags at the time of this check, so `2.3.7` is the latest real stable) -- its peer range, `"react": "^16.8 || ^17.0 || ^18.0 || ^19.0 || ^19.0.0-rc"`, cross-checked against this app's actual `react@^19.2.8`, the same discipline ADR-0031/ADR-0039/CAT-11 each used for their own additions. A second cross-check specific to this dependency: `@radix-ui/react-select@2.3.7`'s own `dependencies` pin `@radix-ui/react-slot` at exactly `1.3.3` -- identical to what `apps/web/package.json` already carries (`^1.3.3`, installed from WEB-5) -- so this addition doesn't introduce a second, differently-versioned copy of a primitive this app already depends on.
  `CategorySelect.tsx` is styled entirely against `design/tokens.ts`'s existing classes (`border-border`, `bg-surface-secondary`, `text-ink`, `accent-wash`, etc.) rather than Radix's unstyled defaults -- the same "restyle the primitive, don't ship its default look" approach WEB-5's shadcn components already established. One Radix-specific quirk handled inside the component rather than pushed onto callers: `<Select.Item>` throws if given `value=""` (Radix reserves the empty string to mean "no selection"), but WEB-10's "All categories" filter option is a real, meaningful `""` value in this app's own filter state -- `CategorySelect` translates `""` to an internal sentinel string at its own boundary (both directions), so neither caller (`ReviewCategoryControl.tsx`, `TransactionsPage.tsx`) has to know Radix's constraint exists.
  **Same `pnpm install` gap as every dependency this project has added (ADR-0031/ADR-0039/CAT-11):** confirmed again this session that `@radix-ui/react-select` is not present anywhere under `apps/web/node_modules` (including the pnpm store), and `uname -a` from this session's device-bridge shell still reports the same Linux aarch64 VM bind-mounting the user's actual Mac `node_modules` -- so `pnpm install` was deliberately not run from here; `package.json` was hand-edited with the registry-checked version, and `apps/web`'s `tsc --noEmit` reports exactly two errors, both in `CategorySelect.tsx` and both attributable solely to this one missing package: `Cannot find module '@radix-ui/react-select'` plus one cascading `TS7006` (`next` implicitly has an `any` type) on `<Select.Root>`'s `onValueChange` callback -- the same "real error plus one cascading noImplicitAny that contextual typing resolves automatically" shape ADR-0039's `react-plaid-link` addition hit, not a second, independent bug. Both are expected to disappear once the user runs `pnpm install` themselves. Notably, `lucide-react` (CAT-11) and `react-plaid-link` (WEB-7) *are* both present in `node_modules` this session -- confirming the user has run a real `pnpm install` on their own Mac since CAT-11 shipped -- which is exactly why this is a fresh, expected gap for this one new package rather than a sign the install habit has broken down.
- **ADR-0045 — `Category.kind` (CAT-16) is `required` with a schema-level default at the Mongoose layer, matching `isDefault`'s own shape, but stays optional (`kind?: CategoryKind`) at the TypeScript layer despite that -- because a `.lean()` read of a row written before this field existed genuinely comes back with `kind` physically absent, not defaulted.** Worth its own entry rather than only a `Category.kind` doc comment, since this is a real Mongoose gotcha with no precedent recorded anywhere else in this decision log: a schema-level `default` only applies when a document is actually constructed through Mongoose (`Model.create()`, or a hydrated `Document` being saved) -- it does NOT retroactively apply to a pre-existing row, and critically, `Model.find().lean()` (what every read in `CategoryRepository` uses, including `findActiveByUser()`) skips Mongoose's document-hydration path entirely, so a `.lean()` read of an old row sees exactly what's stored, `kind: undefined`, never the schema's `"expense"`. This matters here specifically because `kind` is `required` in the schema (unlike `icon`, which is genuinely optional both places) -- a naive reading of `required: true, default: "expense"` would assume every row, old or new, is safe to read as a concrete `"income" | "expense"` with no further care. It isn't, until that row is re-saved through a real write (`CategoryRepository.update()`). Two follow-on decisions this forced: (1) every caller that needs a concrete value (`GET`/`PATCH /api/categories`'s response shape, `CategoriesPage.tsx`'s Income/Expense grouping) normalizes with `kind ?? "expense"` explicitly at its own boundary rather than trusting the schema default to have already done it; (2) no migration script was written to backfill existing rows -- the same "every *new* write stores a real value going forward, no migration needed" precedent `icon` (CAT-11) already established for this codebase, extended here to a `required` field specifically because a migration would mean writing and testing a one-off script for a gap that self-heals the moment a row is touched by `update()` again. **Known consequence, not fixed by this ADR or by CAT-16's page**: a user whose "Income" category was seeded before CAT-16 shipped has a stored row with no `kind` at all, reads back as `"expense"` via the normalization above, and shows under Expense on `/categories` until someone `PATCH`es it with `kind: "income"` -- CAT-16's own delivery note flags this as a known limitation its inline edit control (scoped to color + icon only, by that ticket's own ask) doesn't close.
